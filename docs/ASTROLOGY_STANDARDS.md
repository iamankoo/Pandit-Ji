# Pandit Ji Astrology Standards

Status: **LOCKED canonical location** — `docs/ASTROLOGY_STANDARDS.md` is the single, authoritative document for calculation standards, Vedic defaults, ayanamsa, zodiac, house systems, ephemeris configuration, astronomical calculation requirements, astrology methodology separation, interpretation standards, reproducibility, uncertainty, and versioning. There is no separate `docs/calculation-standards.md` — any earlier reference to that filename referred to this document and should be treated as resolved in favor of this one.

Version: 1.21.0 (Phase 9 closure and evidence hardening: v1.21.0 Raman Shadbala profile, WP-G facts object, Tarot provenance and evidence-bundle integration; earlier v1.15.0 KP, v1.16.0 Shadbala, v1.17.0 Jaimini WP-G, v1.18.0 Chinese Four Pillars, v1.19.0 Tarot and v1.20.0 research-only/deferred systems, on top of the v1.14.0 WP-D Western lock and all earlier locks listed in §Versioning). The document version is not re-stamped onto earlier results: each result keeps the standards version it was produced under (Phase 5 Kundli 1.3.0, Phase 6 rule evaluation 1.4.0, Phase 7 Dasha 1.5.0, Phase 8 transit 1.6.0, Ashtakavarga facts 1.8.0, Western facts 1.14.0, KP facts 1.15.0, Shadbala facts 1.16.0, Chinese pillars 1.18.0, Tarot layouts 1.19.0, Raman Shadbala and WP-G Jaimini facts 1.21.0; the v1.9.0-v1.13.0, v1.17.0 and v1.20.0 locks record no `standards_version` of their own).

This document defines **standards and methodology contracts**, not implementations. Every section below states what a later phase's engine must compute and how, not the engine itself. Each section names the `Phases.md` phase responsible for the actual implementation.

## Core rule
**Facts flow one direction; AI only narrates.**

The AI must never independently calculate planetary positions, houses, ascendant, nakshatra/pada, divisional charts, dashas, transits, yogas/doshas, Panchang timings, Muhurta windows, numerology numbers, or compatibility scores.

## AI scope boundary

- **Self-hosted requirement**: Pandit Ji's core astrology reasoning (chart interpretation, prediction, agent planning, personalization, knowledge reasoning, core chatbot responses) must not depend on external hosted proprietary LLM APIs. See `research/AI_MODELS.md` and `docs/ARCHITECTURE.md` §16's `LLMProvider`/AI Reasoner interface for the technical mechanism; this document fixes it as a standing product requirement, not merely an implementation preference.
- **Fact-vs-narration boundary**: the AI consumes structured facts and evidence bundles produced by the calculation engine, rule engine, and knowledge base (defined in this document and `docs/ARCHITECTURE.md`); it narrates and explains them, and never originates them. This is the same invariant as the Core rule above, restated as an explicit AI-scope requirement per Phase 1 exit criteria.

## Language and multilingual interaction

Standards and product-interaction requirement, locked per explicit project-owner decision (v1.4.0). It is not implemented in Phase 6; the Agent phase (`Phases.md` Phase 15) and the UI phases implement it.

- **Language understanding**: Pandit Ji's AI must understand English, Hindi, Hinglish, Romanized Hindi, mixed English/Hindi and natural code-switching (including within one conversation). This applies regardless of the UI language. There is **no AI language toggle** and no separate English, Hindi or Hinglish AI modes.
- **UI language**: controlled separately through Account → Language. Supported UI languages are English and हिन्दी. New users default to English, and the selected UI language persists per account. Changing the UI language does not automatically force the AI's response language, and the UI never changes because of the language the user types.
- **AI response language**: AI language understanding is multilingual by default. UI language selection is independent from AI language understanding. Agent-phase response-language behavior is defined in the Agent phase and is not defined here.
- **Language-neutral facts**: canonical astrology facts, rules, evidence and calculations use stable identifiers and structured tags, never language-specific phrases; language is realized only at the interaction and presentation layers.
- This section adds no model-specific, provider-specific, UI-implementation or language-classification requirements.

## Default Vedic profile
- Zodiac: Sidereal
- Ayanamsa: Lahiri
- House baseline: Vedic whole-sign
- Degree-level planetary positions, with sufficient underlying precision (minutes/seconds) retained internally for reproducibility even where a UI rounds for display
- Explicit versioned calculation configuration (see §Reproducibility)

Alternate systems (Western/Tropical, KP, Lal Kitab, Nadi, etc.) must be selected explicitly per calculation — never silently substituted for the Vedic default.

### Time standards
Birth time must be normalized correctly and never silently guessed when critical information is missing. Required handling:
- Timezone at the birth location and date (not the user's current timezone)
- Historical timezone changes (a location's UTC offset may have differed from its present-day offset on the birth date)
- DST where applicable for that location/date
- Conversion to UTC before any ephemeris calculation, with local civil time retained alongside it for display/audit
- If the user provides an approximate time (see §Birth-time uncertainty), that approximation must be preserved through the whole pipeline, not resolved into a false-precision exact timestamp

### Location standards
Birthplace must be represented using latitude, longitude, timezone, and historical timezone/DST information where required. Missing or ambiguous critical location/time data must be surfaced to the user for clarification, never silently guessed or defaulted.

## Astrology systems (methodology separation)

Pandit Ji supports multiple astrology methodologies. **Systems must not be silently mixed** — a Vedic answer must never silently apply Western, KP, Lal Kitab, or Nadi rules, and vice versa. Every calculation and every rule/interpretation must be tagged with the system it belongs to. When methodologies are intentionally compared for a user, both must be explicitly labeled as such.

For each system: system identity, zodiac/basis, house methodology (where applicable), core calculation assumptions, major interpretive methodology, source/tradition separation, and configuration/versioning are defined below. Detailed rule content and the actual engines are implemented in later phases as noted; this section fixes the assumptions those phases must implement against.

### Vedic / Jyotish (default)
- Zodiac: Sidereal (Lahiri ayanamsa, per Default Vedic profile above)
- Houses: Vedic whole-sign
- Core assumptions: Parashari framework — divisional charts (§Divisional charts), Vimshottari Dasha (§Vimshottari Dasha), Nakshatra-based timing (§Nakshatra standards), Parashari yoga/dosha rules (§Yoga/Dosha standards)
- Interpretive methodology: classical Parashari/Jaimini traditions, cited per rule (see `research/ASTROLOGY_SOURCES.md`)
- Implemented across Phases 4-8 (calculation foundation, Kundli engine, rule engine, Dasha/transit engine)

### Western / Tropical
- Zodiac: Tropical (no ayanamsa/precession correction — this is the defining difference from the Vedic sidereal profile, not an alternate ayanamsa value)
- Houses: Placidus (default Western house system)
- Core assumptions: same underlying ephemeris/planetary-position engine as Vedic, computed against the tropical zero point instead of sidereal; same aspect-detection mechanism, tropical orb conventions *(Phase 1 wording, uncited; superseded by §Western standards v1.14.0: Western aspects are degree-and-orb aspects of their own (WD-09 to WD-14) and share no mechanism with Vedic graha drishti, which is sign-based)*
- Interpretive methodology: traditional Western natal astrology by default; psychological/humanistic framing is a configurable interpretive layer, not a calculation difference
- Source/tradition separation: Western interpretive rules must never be applied to a Vedic (sidereal) chart or vice versa
- Configuration: a distinct `calculation_config` row (`zodiac_type=tropical`, `house_system=placidus`)
- Implemented in Phase 9 (Advanced Astrology Systems) — WP-D, see §Western standards (v1.14.0, WD-01 to WD-20)

### KP (Krishnamurti Paddhati)
- Zodiac: Sidereal, but using the **Krishnamurti ayanamsa**, not Lahiri — this must be a distinct, explicit configuration value; KP must never silently reuse the Vedic default's Lahiri ayanamsa
- Houses: Placidus cusps (KP's traditional house basis)
- Core assumptions: the sub-lord theory — each nakshatra (already divided by the 9 Vimshottari lords) is further subdivided into 249 sub-divisions using the same 9-lord proportional scheme applied recursively (sub-lord, and traditionally sub-sub-lord); significators are derived from house/cuspal sub-lord analysis
- Interpretive methodology: KP significator and ruling-planet analysis, distinct from Parashari yogas
- Source/tradition separation: KP must not reuse Parashari yoga/dosha rules; its own rule set is separate
- Implemented in Phase 9
- Note: the sub-lord subdivision arithmetic must be validated against an authoritative KP reference before implementation (see `docs/ARCHITECTURE.md` §30) *(done in Phase 9 WP-E: see §KP standards, v1.15.0, KP-01 to KP-16; the "sub-sub" level is a derived calculation, and KP rule sets beyond significators and Ruling Planets are not implemented)*

### Lal Kitab
- Basis: differs fundamentally from Parashari Vedic astrology — traditional Lal Kitab practice does not use the varga (divisional chart) system or nakshatra-based timing the way Parashari Jyotish does, and historically some variants work from a simplified, fixed house-based chart representation rather than degree-precision positions
- Core assumptions: **provisional, pending dedicated source validation** — this document does not assert a final Lal Kitab calculation methodology as settled fact. An astrology-literate reviewer must validate the exact chart-construction and remedy-derivation rules against recognized Lal Kitab texts (`research/ASTROLOGY_SOURCES.md`) before Phase 9 implements it
- Interpretive methodology: remedies are the central interpretive output in Lal Kitab practice; remedy content must be sourced and tagged the same way as any other traditional rule (§Yoga/Dosha standards' source/tradition requirement applies equally here)
- Source/tradition separation: Lal Kitab houses/rules must never be silently combined with Parashari Vedic houses/rules, even though both may present a similar 12-house layout
- Implemented in Phase 9, after research validation *(Phase 9 research outcome, v1.20.0 P9-01: not implemented; no edition could be read; deferred)*

### Nadi
- Basis: Vedic sidereal, but traditional Nadi Jyotish practice relies on extremely precise birth time and, historically, matching a chart to pre-recorded leaf texts at a fine-grained subdivision finer than the standard varga scheme
- Core assumptions: **provisional, pending dedicated source validation**, for the same reason as Lal Kitab above — generic rule-based computation may not faithfully represent traditional Nadi practice, which is traditionally text-matching rather than generic rule evaluation. This document intentionally does not assert a specific Nadi computational methodology as final.
- Source/tradition separation: Nadi timing/interpretation must not be silently substituted for or merged with standard Parashari Vimshottari timing
- Implemented in Phase 9, after research validation *(Phase 9 research outcome, v1.20.0 P9-02: leaf-matching is not a deterministic calculation; not implemented)*

### Numerology, Chinese astrology, Tarot, Vastu/Feng Shui, Horary, Palmistry
- Numerology: see the dedicated §Numerology standards section below (implemented Phase 11)
- Chinese astrology, Tarot, Vastu/Feng Shui, Horary: system identity acknowledged as in-scope per `features.md`; each requires its own dedicated methodology definition before implementation, following the same pattern as the systems above (system identity, basis, core assumptions, source/tradition separation, non-mixing rule); detailed standards for these are deferred to the phase that implements them (Phase 9 for KP/Lal Kitab/Nadi/Western/Chinese, Phase 17 for cross-domain analysis touching Vastu) and must be written before that phase begins, following this document's format
- Phase 9 outcome (v1.18.0-v1.20.0): Chinese astrology is implemented as the Four Pillars calendar foundation (§Chinese standards); Tarot as a deck/spread/draw foundation (§Tarot standards); general Horary, Vastu and Feng Shui are deferred (§Phase 9 closure, P9-03 to P9-05)
- Palmistry: AI Palm Reading is a locked product feature (`features.md` §34); see §Palmistry below for the interpretation-boundary standard, and `Phases.md` Phase 13 (Palm Reading & Vision Intelligence) for the dedicated vision-pipeline/rule-layer implementation

## Divisional charts (Vargas)

Defines the standard and contract that **Phase 5 (Birth Chart / Kundli Engine)** implements — not implemented in Phase 1.

- **D1 / Rashi chart**: the base birth chart — each planet's sidereal longitude placed directly into its zodiac sign and house, using the Default Vedic profile's ayanamsa/house baseline. All other divisional charts are derived from this same set of longitudes; D1 is the foundation, not a division.
- **D9 / Navamsa**: each of the 12 signs is divided into 9 equal parts of 3°20′ each; a planet's Navamsa sign is determined by classical Parashari rule based on which 3°20′ segment its longitude falls into and the modality (movable/fixed/dual) of its D1 sign, per the standard starting-point convention. Represents marriage, spouse, dharma, and the inner strength of D1 placements.
- **Divisional-chart framework**: Pandit Ji's locked varga set is the classical Shodashvarga (16 principal divisional charts): D1, D2, D3, D4, D7, D9, D10, D12, D16, D20, D24, D27, D30, D40, D45, D60. Each varga is derived deterministically from the same D1 sidereal longitudes via its classical division formula — no varga is independently observed or separately calculated from the ephemeris.
- **What each chart represents** (canonical significations, to be expanded per-varga during Phase 5): D1 = self/body/overall life; D2 (Hora) = wealth; D3 (Drekkana) = siblings/courage; D4 (Chaturthamsa) = property/home/fortune; D7 (Saptamsa) = children/progeny; D9 (Navamsa) = marriage/spouse/dharma; D10 (Dasamsa) = career/profession; D12 (Dwadasamsa) = parents; D16 (Shodasamsa) = vehicles/general happiness; D20 (Vimsamsa) = spiritual pursuits; D24 (Chaturvimsamsa) = education/learning; D27 (Nakshatramsa/Bhamsa) = strengths/weaknesses; D30 (Trimsamsa) = misfortunes/challenges; D40 (Khavedamsa) = auspicious/inauspicious effects; D45 (Akshavedamsa) = general life conduct; D60 (Shashtiamsa) = overall past-karma/fine-grained life analysis.
- **Chart-specific applicability**: which varga is authoritative for which life-domain question is determined by the domain-to-varga mapping used in `docs/ARCHITECTURE.md` §8's Astrology Planner (e.g., a marriage question requires D1 + D9; a career question requires D1 + D10). This mapping is finalized during Phase 5/Phase 17, not Phase 1.
- **Versioning/configuration**: the varga division scheme (classical Parashari, as fixed above) is part of `calculation_config`; if an alternate varga scheme is ever supported, it must be a distinct, explicit configuration value, never silently substituted.

### Varga derivation formulas

Locked per explicit project-owner decision (see §Versioning change log) during Phase 5's mandatory standards audit. Each formula below is the classical Parashari (Brihat Parashara Hora Shastra) derivation, applied to a planet's precise D1 sidereal longitude and sign. "Same sign" always means the planet's own D1 sign; sign counting is always forward/zodiacal.

- **D1 (Rashi)**: identity — see above.
- **D2 (Hora)**, 2 × 15°: odd D1 sign — 0-15° = Sun's Hora (maps to Leo), 15-30° = Moon's Hora (maps to Cancer); even D1 sign — reversed (0-15° = Moon's Hora/Cancer, 15-30° = Sun's Hora/Leo).
- **D3 (Drekkana)**, 3 × 10°: 0-10° = same sign; 10-20° = 5th sign from it; 20-30° = 9th sign from it (BPHS trine-counting rule — the "sequential" alternate convention some software uses is explicitly not adopted).
- **D4 (Chaturthamsa)**, 4 × 7°30′: parts land on the same, 4th, 7th, and 10th sign from the D1 sign, in that order (kendra/quadrant counting).
- **D7 (Saptamsa)**, 7 × 4°17′8.571…″: odd D1 sign — count of 7 consecutive signs starts at the same sign; even D1 sign — starts at the 7th sign from it.
- **D9 (Navamsa)**: see above.
- **D10 (Dasamsa)**, 10 × 3°: odd D1 sign — count of 10 consecutive signs starts at the same sign; even D1 sign — starts at the 9th sign from it.
- **D12 (Dwadasamsa)**, 12 × 2°30′: count of 12 consecutive signs always starts at the same sign (no odd/even distinction).
- **D16 (Shodasamsa)**, 16 × 1°52′30″: count of 16 consecutive signs (cycling the zodiac as needed) starts at Aries for movable D1 signs, Leo for fixed, Sagittarius for dual.
- **D20 (Vimsamsa)**, 20 × 1°30′: count starts at Aries for movable D1 signs, Sagittarius for fixed, Leo for dual.
- **D24 (Chaturvimsamsa / Siddhamsa)**, 24 × 1°15′ (two full zodiac cycles): odd D1 sign — count starts at Leo; even D1 sign — starts at Cancer.
- **D27 (Nakshatramsa / Bhamsa)**, 27 × 1°6′40″: count starts at Aries for movable D1 signs, Cancer for fixed, Libra for dual.
- **D30 (Trimsamsa)**, 30 divisions, non-equal spans: odd D1 sign — Mars rules 0-5° (maps to Aries), Saturn 5-10° (Aquarius), Jupiter 10-18° (Sagittarius), Mercury 18-25° (Gemini), Venus 25-30° (Libra); even D1 sign — Venus rules 0-5° (Taurus), Mercury 5-12° (Virgo), Jupiter 12-20° (Pisces), Saturn 20-25° (Capricorn), Mars 25-30° (Scorpio).
- **D40 (Khavedamsa)**, 40 × 0°45′: odd D1 sign — count starts at Aries; even D1 sign — starts at Libra.
- **D45 (Akshavedamsa)**, 45 × 0°40′: count starts at Aries for movable D1 signs, Leo for fixed, Sagittarius for dual.
- **D60 (Shashtiamsa)**, 60 × 0°30′: count of 60 divisions always starts at the same sign (sequential, no odd/even or modality distinction). **Scope limitation, locked explicitly rather than guessed**: this standard defines the sign/degree placement only. The classical 60-named-deity assignment per division is not asserted here and is not implemented in Phase 5 — a documented limitation, not a silent omission.

All fourteen formulas above are locked as of this version. Each is a distinct, explicit, versioned configuration value under `calculation_config`'s varga-scheme setting (§Reproducibility) — an alternate scheme for any one of them must never silently replace the formula recorded here.

## Vimshottari Dasha

Defines the standard and contract that **Phase 7 (Dasha & Timing Engine)** implements. The open conventions left to Phase 7 are locked in §Phase 7 methodology lock below (v1.5.0).

- **Total cycle**: 120 years, distributed across nine planetary lords in a fixed sequence.
- **Nine planetary lords and fixed durations** (years): Ketu 7, Venus 20, Sun 6, Moon 10, Mars 7, Rahu 18, Jupiter 16, Saturn 19, Mercury 17 (sums to 120).
- **Sequence**: Ketu → Venus → Sun → Moon → Mars → Rahu → Jupiter → Saturn → Mercury → (cycles back to Ketu).
- **Starting Mahadasha lord**: the lord of the nakshatra occupied by the natal Moon at birth (see §Nakshatra standards for the nakshatra-lord sequence).
- **Balance of the first Mahadasha**: determined by how far the Moon has progressed through that nakshatra at the birth instant — the remaining fraction of the nakshatra's 13°20′ span, converted to time as that same fraction of the starting lord's full dasha-year duration.
- **Hierarchy**: Mahadasha → Antardasha → Pratyantar (Sookshma and Prana are out of scope; see §Phase 7 methodology lock) are nested applications of the same nine-lord sequence and proportional-duration logic, each level starting from its parent period's own lord and subdividing the parent's duration across the nine lords in the same fixed sequence and proportion.
- **Exact timestamps and boundary conditions**: all dasha transitions are computed as precise moments (date and time), not whole-day boundaries, derived from the birth instant plus cumulative duration — boundary dates must be computed to at least day-level precision, with the exact moment retained internally for reproducibility.
- **Reproducibility**: identical birth data and calculation configuration must reproduce an identical Mahadasha/Antardasha/Pratyantar timeline, byte-for-byte, every time.
- **Ownership of open Vimshottari conventions (v1.4.0 clarification)**: the balance of the first Mahadasha (BPHS Ch. 46 v. 16 measures the Moon's stay in the nakshatra by time; this standard uses the Moon's longitude fraction), the year length (no verse states it; the translator's worked examples imply 30-day months), leap-year handling and boundary rounding remain **Phase 7** decisions. Phase 6 must not calculate them; it only consumes Phase 7 dasha lords (§Phase 6 rule-engine methodology). **Resolved in v1.5.0**: see §Phase 7 methodology lock (explicit profiles; the balance method stays a documented source conflict).

### Phase 7 methodology lock (v1.5.0)

Locked per explicit project-owner direction for the Phase 7 implementation. It fixes explicit **profiles** where the sources are silent or in conflict. **None of these profiles is presented as the only valid classical method.** Source-supported statements, translator notes, inferences and Pandit Ji engineering conventions are labelled separately and never merged; every result records the profile IDs it used.

- **Scope**: Vimshottari Dasha to the Pratyantar level (Mahadasha, Antardasha, Pratyantar), with start and end instants, current, past and future lookup, and transitions. The engine produces **temporal facts and their provenance only**. It does not interpret: no life-domain reading (career, marriage, finance, education, health, relationships), no planetary-result or house-based prediction, and no Maraka interpretation. **Maraka timing is a later rule-engine consumer of these facts, not Phase 7 scope.** Out of scope: Ashtottari, Yogini, Chara, Narayana and every other Dasha system; Sookshma and Prana; birth-time rectification or any automatic time correction.
- **Sequence and years** (source-supported at translation level, no Sanskrit-level verification): Sun 6, Moon 10, Mars 7, Rahu 18, Jupiter 16, Saturn 19, Mercury 17, Ketu 7, Venus 20 (120), counted from Krittika (BPHS Ch. 46 v. 12–15, Kapoor, image-checked; Phaladeepika Adhyaya XIX sl. 2, OCR-level). The sequence is the one already locked in §Nakshatra standards; the implementation derives it from the Nakshatra-lord table so there is a single source of truth.
- **Balance at birth**: this is a **documented source conflict** and this standard does not resolve it. BPHS Ch. 46 v. 16 (Kapoor, translation level) measures the Moon's stay in the nakshatra as Panchanga time (ghatis and palas); the translator's note says modern researchers use the Moon's longitude; Phaladeepika XIX sl. 3 (OCR-level) uses a divisor of 60 without settling whether the unit is time or arc; the Uttara Kalamrita worked example (translator, OCR-level) differs by seven days between the two methods. Profiles:
  - `DASHA_STANDARD_V1_BALANCE_LONGITUDE` — **the Pandit Ji product default**, an engineering convention, not a classical source statement. remaining_fraction = 1 − (fraction of the birth Nakshatra already traversed by the Moon's sidereal longitude), computed by exact rational arithmetic; balance = remaining_fraction × the starting lord's full Mahadasha. It uses the Nakshatra classification convention of §Nakshatra standards (v1.4.1).
  - `DASHA_BPHS_KAPOOR_46_16_BALANCE_TIME` — documented, **inactive**: it needs the Moon's Nakshatra entry and exit instants, which are not computed. It is not approximated.
  - `DASHA_PHALADEEPIKA_SASTRI_XIX_3_BALANCE` — documented, **inactive**: the time-versus-arc reading of the source is unresolved.
- **Year length**: no BPHS verse read states it. Phaladeepika XIX sl. 4 (OCR-level) calls the Sun's return one solar year, also the Ududasa year; applying that to Vimshottari is an **inference**. Profiles are fixed-duration years with **no calendar-year or leap-year arithmetic**:
  - `YEAR_365_2425_FIXED_DAY` — **the Pandit Ji product default** (365.2425 mean solar days), an engineering convention. It equals a whole number of seconds (31,556,952).
  - `YEAR_365_25_FIXED_DAY` and `YEAR_360_FIXED_DAY` (a translator worked-example convention: months counted as 30 days) — active, selectable, never silently substituted.
  - `YEAR_SIDEREAL_365_256363_FIXED_DAY` (translator note, taken from the project research record and not re-verified) and `YEAR_SUN_RETURN_PHALADEEPIKA_XIX_4` (variable per birth; an inference) — documented, **inactive**.
- **Sub-periods**: profile `DASHA_SUBPERIOD_PROPORTIONAL_FULL_PARENT_V1`. Child duration = full nominal parent duration × child lord's years ÷ 120, children in the canonical sequence starting from the parent's own lord (BPHS Ch. 51 v. 1–2 and Ch. 61 v. 1, OCR-level in the registry). **How the birth-balance Mahadasha is subdivided is not stated in the sources read.** The engineering convention is that the balance Mahadasha keeps the sub-periods of the **full, untruncated** Mahadasha: sub-periods that ended before birth are omitted and the one containing birth starts at birth, flagged `truncated_at_birth` (with its untruncated `nominal_start_utc` preserved). The alternative of scaling the sub-periods to the balance length was not implemented. This treatment is recorded for explicit owner confirmation.
- **Time**: UTC is canonical. All arithmetic is exact rational arithmetic in microseconds from the birth instant; each boundary is serialized once as `floor` to a whole microsecond (at most one microsecond of quantization, applied identically to every shared boundary, so periods tile with no gap and no overlap). Local civil time is never used for duration arithmetic and is kept only as recorded input, with the IANA timezone, DST flags and ambiguity policy. Julian Day UT of the birth instant is retained from the existing time resolution.
- **Boundary convention** (a Pandit Ji engineering convention; no classical source read states an inclusivity rule): all Nakshatra, Pada and Dasha intervals are half-open, [start, end). A shared boundary belongs to the **later** period; the overall timeline end is exclusive.
- **Birth-time precision**: `EXACT`, `APPROXIMATE` or `NOT_EVALUABLE`. An approximate time is never treated as exact. An approximate time needs an explicit symmetric uncertainty interval; without one the result is `NOT_EVALUABLE(uncertainty_interval_missing)`. If the interval reaches a Nakshatra boundary (a closed interval, so touching counts) the starting lord is ambiguous and the result is `NOT_EVALUABLE(starting_lord_ambiguous)` with no timeline. If the starting lord is stable the result is `APPROXIMATE`, with the Moon's longitude range, the balance-fraction range and the range of the first Mahadasha's end (evaluated at the two interval endpoints and the nominal instant, not a proven bound); a Pada boundary inside the interval is a warning only. No rectification and no inference of an exact time.
- **Statuses and reason codes**: `SUCCESS`, `APPROXIMATE`, `NOT_EVALUABLE`, `INVALID_INPUT`, `UNSUPPORTED_PROFILE`, `CONFIGURATION_ERROR`, `INTERNAL_ERROR`. Every non-success result carries a machine-readable reason code and no periods; nothing is guessed. Sookshma-depth requests are rejected as `unsupported_hierarchy_depth`.
- **Facts contract**: `astro-engine` calculates and owns `DashaFacts` (system, status, profile IDs, boundary convention, precision, starting Nakshatra/Pada/lord, elapsed and remaining fractions, remaining duration, the period tree with UTC boundaries, warnings and labelled provenance). The rule engine never calculates a Dasha; it records the facts in the evidence bundle as an additive optional `dasha` section. No shipped Phase 6 rule reads Dasha facts yet, so the reserved `requires_dasha` reason is not emitted by any current rule.
- **Standards versions**: Phase 7 results record `standards_version = 1.5.0`. The Phase 5 Kundli constant (`STANDARDS_VERSION` in `kundli.py`) is **not** advanced and stays `1.3.0`: it names the standards whose formulas the Kundli implements, and v1.4.1 corrected an implementation defect against the 1.3.0 text (the standard already said to divide by 13°20′). Phase 6 rule evaluation keeps recording its own ruleset standards version.

## Transit / Gochar standards (Phase 8 methodology lock, v1.6.0)

Defines the standard and contract that **Phase 8 (Transit / Gochar Engine)** implements. Locked per explicit project-owner approval of the Phase 8 methodology report (Approval Gate 1, all nine recommendations). It fixes explicit **profiles** where sources are silent or in conflict. **No profile is presented as the only valid classical method.** Source-supported statements, translator notes, inferences, derived calculations, modern traditions and Pandit Ji engineering conventions are labelled separately and never merged; every result records the profile IDs it used. Evidence labels used in results: `source_supported`, `translator_note`, `inference`, `engineering_convention`, `derived_calculation`, `unresolved_conflict`, `modern_tradition`, `engineering_evidence`.

All source statements below are at **translation level**. No Sanskrit-level verification exists for any of them. Verification levels are recorded per item in `research/ASTROLOGY_SOURCES.md` (Phase 8 group).

- **TR-01 Scope.** Deterministic transit **facts and provenance only**, like Phase 7. The engine reports where a planet is, what it is doing, what structural relations it has to the natal Moon (and optionally the Lagna), and when its sign, station or (opt-in) Nakshatra state changes. It does not interpret. Out of scope: effect or prediction text, good or bad verdicts, remedies, alerts and notifications, Ashtakavarga scoring, Sarvatobhadra Chakra, Latta, half-sign or decanate "effectiveness", degree-based or orb-based contacts, applying and separating aspects, Dhaiya, Ashtama Shani and degree-based Sade Sati variants (reserved profile IDs only), birth-time rectification, HTTP endpoints and database tables (Phase 18 owns them; `docs/ARCHITECTURE.md` §26).
- **TR-02 Reference point: `TRANSIT_REF_MOON_SIGN` (default).** Houses are counted whole-sign from the natal Moon's sidereal sign, the Moon's sign being house 1. Source-supported at translation level: Phaladeepika Adhyaya XXVI sl. 1 (page-image checked) states that the Moon's Lagna is the reference for transit effects; Brihat Samhita Adhyaya CIV sl. 4 (page-image checked) counts "from the Janma Rasi"; the Brihat Jataka Ch. IX translator's commentary says the same (translator note). No source read gives a Lagna-based Gochar table.
- **TR-03 Optional Lagna fact: `TRANSIT_REF_LAGNA_SIGN`.** A positional fact only (whole-sign house of the transiting planet counted from the natal Lagna sign), labelled `engineering_convention`, never classical. It needs an exact natal time: otherwise `NOT_EVALUABLE(lagna_unavailable)`. No favourable-set, Vedha or verdict is attached to it.
- **TR-04 Favourable-house sets and the Moon-from-Moon conflict.** "Favourable set" means membership in a source's list of houses from the natal Moon; it is a structural fact, not a verdict. Each source is a separate reading with its own ID, evaluated separately:
  - `GOCHARA_FAVOURABLE_PHALADEEPIKA_SASTRI_XXVI_2` (source-supported; page-image checked): Sun 3, 6, 10; Moon 1, 3, 6, 7, 10; Mars and Saturn 3, 6; Mercury 2, 4, 6, 8, 10; Jupiter 2, 5, 7, 9; Venus every house except 6, 7, 10; and, for all planets, the 11th; Rahu and Ketu "similar to the Sun".
  - `GOCHARA_FAVOURABLE_BRIHAT_SAMHITA_SASTRI_CIV_4` (source-supported; page-image checked): the same sets as Phaladeepika for the seven planets; it does not mention the nodes.
  - `GOCHARA_FAVOURABLE_BRIHAT_JATAKA_SASTRI_IX_1_7` (source-supported; page-image checked): the benefic places from the Moon in each planet's Ashtakavarga: Sun 3, 6, 10, 11; Mars 3, 6, 11; Mercury 2, 4, 6, 8, 10, 11; Jupiter 2, 5, 7, 9, 11; Venus 1, 2, 3, 4, 5, 8, 9, 11, 12; Saturn 3, 6, 11; **Moon 1, 3, 5, 7, 10, 11**.
  - `GOCHARA_FAVOURABLE_BPHS_KAPOOR_66_DERIVED` (`derived_calculation`, not a source statement): the complement of the dot houses that the Moon contributes in each planet's Ashtakavarga chart (BPHS Ch. 66 v. 16–42, OCR-level lists; only p. 847, the Moon's chart, was page-image checked). It agrees with the others for the six non-Moon planets and gives **Moon 1, 3, 6, 7, 9, 10, 11**.
  - **Unresolved source conflict (Moon from the Moon):** the readings differ on houses 5 (Brihat Jataka only), 6 (all but Brihat Jataka) and 9 (BPHS-derived only). Houses 1, 3, 7, 10, 11 are in every Moon reading and houses 2, 4, 8, 12 are in none. For the Moon in house 5, 6 or 9 the consolidated result is `NOT_EVALUABLE(reading_ambiguous)` with every reading's outcome retained. No reading is chosen. Whether this is a textual variant or a translation matter cannot be settled without qualified Sanskrit review.
  - For the six non-Moon planets all four readings agree; the consolidated result carries every attesting reading ID.
- **TR-05 Rahu and Ketu.** Positional facts are calculated normally (Mean Node default, True Node an explicit alternate through the Phase 4 configuration; Ketu = Rahu + 180°; retrograde by speed sign, so Mean Node is always retrograde and has no stations). The only favourable-set statement found is Phaladeepika sl. 2 ("similar to the Sun"), single-source; a Jataka Parijata commentary quote of another work may say the opposite (unreviewed OCR of a Sanskrit line, not relied on). Therefore the nodes are evaluated only under the single-source profile `GOCHARA_FAVOURABLE_PHALADEEPIKA_SASTRI_XXVI_2`, flagged `single_source`; the consolidated result for a node is `NOT_EVALUABLE(node_reading_single_source)`. Node Vedha as the *subject* is `NOT_EVALUABLE(not_specified_by_source)` because sl. 2 says "similar to the Sun" only about the favourable houses.
- **TR-06 Vedha: `GOCHARA_VEDHA_PHALADEEPIKA_SASTRI_XXVI_3_8`.** Source-supported at verse level in Phaladeepika only (sl. 3–8, page-image checked). Further mentions (Phaladeepika's own footnotes citing Narada and Jataka Parijata, the Jataka Parijata commentary quoting another work, the Brihat Samhita translator's footnote) are cross-references (translator notes, unreviewed), not independent corroboration of the numbers. Table, favourable house to Vedha house: Sun 11→5, 3→9, 10→4, 6→12; Moon 7→2, 1→5, 6→12, 11→8, 10→4, 3→9; Mars and Saturn 3→12, 11→5, 6→9; Mercury 2→5, 4→3, 6→9, 8→1, 10→8, 11→12; Jupiter 2→12, 11→8, 9→10, 5→4, 7→3; Venus 1→8, 2→7, 3→1, 4→10, 5→9, 8→5, 9→11, 12→6, 11→3. Exceptions stated in the verses: Saturn does not obstruct the Sun and the Sun does not obstruct Saturn; the Moon and Mercury do not obstruct each other.
  - **Structural definition.** For a subject planet in a house that has a Vedha pair, the fact records the Vedha house (counted from the natal Moon) and which *transiting* planets occupy that house's sign, excluding the subject and the exempt planet. Natal planets are not occupants (the verses speak of transit). The fact is `vedha_present` true or false; it carries no verdict.
  - **Nodes as occupants.** The verses say "planets" without naming the nodes. If a node occupies the Vedha sign and would change the result (no other occupant is present), the fact is `NOT_EVALUABLE(node_participation_unspecified)`, following the Phase 6 policy (§Node policy). If another occupant already makes the fact true, it is true.
  - **Translation anomaly, preserved.** Sl. 8 (Venus) says Venus "will give bad effects" in the listed houses if marred, whereas the parallel slokas 3–7 speak of good effects when the Vedha place is free. It is unresolved whether this is a translator's slip or a real difference; a warning `translation_wording_anomaly_venus_sl8` is attached to every Venus Vedha fact, and the table is used unchanged.
  - Vedha is a Phaladeepika-specific profile. It is never combined with the Ashtakavarga method.
- **TR-07 Transit-to-natal contacts: `TRANSIT_CONTACT_SIGN_BASED_V1` (`engineering_convention`).** Only sign-based facts: same-sign conjunction of a transiting planet with a natal planet, and the Phase 5 whole-sign graha drishti (§Planetary aspects standard, unchanged) from the transiting planet's sign to the sign of a natal planet. Degree, orb, applying and separating aspects are not produced; `docs/ARCHITECTURE.md` §31's older phrase "transit-to-natal angles" is realised only as these sign-based contacts. Contacts need an exact natal time (`NOT_EVALUABLE` otherwise). A contact changes only at a transiting planet's sign ingress, so window results attach the contacts holding from each ingress instant.
- **TR-08 Events: `TRANSIT_EVENTS_ENGINEERING_V1` (`engineering_convention`).** No source read defines transit "events". Kinds: `SIGN_INGRESS` (including backward re-entry), `STATION_RETROGRADE`, `STATION_DIRECT`, and, opt-in only, `NAKSHATRA_INGRESS`. A station is the instant the sign of the longitude speed changes, retrograde being speed < 0 (the Phase 4 definition). Instants are found by bracketed bisection; every ingress or station instant is a numerical solution and never "exact" (TR-10).
- **TR-09 Sade Sati: `SADE_SATI_SIGN_BASED_MODERN_V1` (`modern_tradition`).** The combined unit was **not found** in the classical texts read (BPHS, Brihat Jataka, Brihat Samhita, Phaladeepika, Jataka Parijata; a keyword probe of unreliable OCR, so `NOT_VERIFIED_BY_OCR`, not proof of absence). Phaladeepika sl. 22 and Brihat Samhita CIV sl. 39–45 give Saturn's per-house effects individually, and describe every house except the 3rd, 6th and 11th from the Moon as unfavourable; a 12th–1st–2nd window is a subset of these, not a stated unit. The profile is a transparent sign-based construct and no classical certainty is implied:
  - Band: Saturn's sidereal sign is the natal Moon sign −1, 0 or +1 (phase 1 = 12th, phase 2 = 1st, phase 3 = 2nd from the Moon).
  - **Segments**: maximal intervals of Saturn in one band sign. They are never merged across a gap.
  - **Episodes**: maximal runs of contiguous segments. An exit and re-entry produces a separate episode, flagged `retrograde_reentry_of_previous_episode` when retrograde motion bounds the gap at either end: the previous episode ended by backward motion, or this episode was entered by backward motion (each segment and episode records `entered_by_backward_motion` and `ended_by_backward_motion`; backward means speed < 0 at the ingress). No "7½ years" duration is asserted; durations are derived only.
  - A segment or episode that starts before, or ends after, the requested window is clipped and flagged; its unknown boundary is `null`.
  - Approximate natal time whose Moon range reaches a sign boundary gives `NOT_EVALUABLE(natal_moon_sign_ambiguous)`.
  - Reserved and **not implemented** (`UNSUPPORTED_PROFILE`): `DHAIYA_SIGN_BASED_MODERN_V1`, `ASHTAMA_SIGN_BASED_MODERN_V1`, `SADE_SATI_DEGREE_45_MODERN_V1`. No Ashtakavarga is mixed in.
- **TR-10 Time, precision and boundaries.**
  - UTC is canonical; local time is display metadata. Positions are calculated at the Julian Day UT of the UTC instant, exactly as Phase 4 does (the sub-second UT1−UTC difference is ignored and documented).
  - **Boundary convention** `half_open_start_inclusive_end_exclusive` (`engineering_convention`, as in Phase 7): a sign or Nakshatra interval is [start, end); the later interval owns a shared boundary; a window is [start, end). Sign classification uses the existing exact convention (`rashi.sign_index_from_longitude`, Nakshatra by `nakshatra_position`).
  - **Solving.** Ingress and station instants use bisection on a bracket that contains exactly one change, to a fixed search tolerance of 1e-8 day (about 0.86 ms). Scan steps (days): Moon 0.25, Mercury 0.5, Venus 1, Sun 2, Mars 2, Jupiter 5, Saturn 5, Rahu and Ketu 5. A step in which the speed changes sign is split at the station, so that two sign crossings around a station inside one step are not missed. The scan grid is anchored to absolute multiples of the step (not to the window), so an event's instant does not depend on the window that contains it; a window scan starts one step before its start (so an ingress exactly at the start is found) and events are reported for the half-open window [start, end). Station instants are limited by the numerical noise of the ephemeris speed near zero (milliseconds), not by the bisection tolerance. An ingress that falls at the window start within the solver tolerance is already reflected in the state at the start, and does not replay as a spurious Sade Sati segment of the previous sign.
  - **Accuracy statement (`engineering_evidence`, not a validation).** Numerical resolution is not accuracy. Every result records the ephemeris mode, the ayanamsa and the solver tolerance, and states that instants are not exact. An independent comparison of the Swiss Ephemeris **tropical** longitude of date (Moshier mode, through the Phase 4 adapter) with JPL Horizons (geocentric apparent ecliptic longitude of date) on 42 samples (7 bodies, 6 dates, 1950–2060; fixture `services/astro-engine/tests/fixtures/horizons_transit_reference.json`, retrieved 2026-09-21) found maximum differences of 0.26″ (Sun), 0.19″ (Mars), 0.15″ (Mercury, Venus), 0.38″ (Jupiter), 0.27″ (Saturn) and 4.78″ (Moon), which correspond to seconds of time at a sign boundary, and to minutes for a slow planet near a station. This checks planetary longitude only, at these samples; it does not validate the Lahiri ayanamsa value or the sidereal frame.
  - **Ayanamsa sensitivity.** A sidereal ingress instant depends on the ayanamsa value. In the same 42 samples the ayanamsa implied by the sidereal calculation (tropical minus sidereal longitude) differed from the value returned by the ephemeris adapter's `get_ayanamsa_degrees` by −15.6″ to +15.7″, the size of nutation in longitude and about 1.5 hours of Saturn's motion; the cause was not investigated (a Swiss Ephemeris frame convention, not shown to be a defect). Published ingress times for the same event differ by hours across Tier 5 pages (Saturn into Pisces, 29 March 2025: two pages give 01:07 and 02:31 IST, the engine gives 21:44:41 IST; a 20-hour difference is about 3.3′ for a planet moving 0.066° per day). Only the date agrees, so published times are never used as golden references; results always record `ayanamsa`.
  - **Approximate natal time.** The Moon-based facts need the natal Moon's sign to be unambiguous: with an approximate natal time an explicit Moon longitude range is required (`NOT_EVALUABLE(natal_range_missing)` otherwise) and a range that reaches a sign boundary gives `NOT_EVALUABLE(natal_moon_sign_ambiguous)`. Positions and events do not depend on the natal chart and are still produced.
- **TR-11 Limits.** A request window may not exceed 200 years (200 × 365.2425 days): `NOT_EVALUABLE(window_too_large)`. A window may not produce more than 50,000 events: `NOT_EVALUABLE(event_limit_exceeded)` with no partial event list.
- **TR-12 Statuses and reason codes.** Statuses: `SUCCESS`, `NOT_EVALUABLE`, `INVALID_INPUT`, `UNSUPPORTED_PROFILE`, `CONFIGURATION_ERROR`, `INTERNAL_ERROR`. Reason codes include `moon_longitude_missing`, `natal_moon_sign_ambiguous`, `natal_range_missing`, `natal_range_inconsistent`, `natal_time_unknown`, `natal_planets_unavailable`, `non_finite_longitude`, `no_query`, `no_bodies`, `lagna_unavailable`, `reading_ambiguous`, `node_participation_unspecified`, `node_reading_single_source`, `not_specified_by_source`, `window_too_large`, `event_limit_exceeded`, `invalid_window`, `invalid_instant`, `ephemeris_unavailable`, `ephemeris_error`, `unsupported_profile`, `sidereal_zodiac_required`. Every non-success result carries a reason code and no facts; nothing is guessed.
- **TR-13 Facts contract.** `astro-engine` calculates and owns `TransitFacts` (system, status, profile IDs, boundary convention, configuration, natal summary, accuracy block, an optional instant snapshot, an optional window with events, an optional Sade Sati section, warnings and labelled provenance). The rule engine never calculates a transit: it records the JSON form in the evidence bundle as an additive optional `transit` section (the bundle serializes and hashes exactly as before when it is absent). No shipped rule reads transit facts. Cache keys (a documented design, not implemented until Phase 18): `(natal_reference_hash, instant or window, profile_ids, configuration_hash, engine_version)`.
- **TR-14 Ownership.** Ashtakavarga scoring and transit scoring by bindus are Phase 9 (BPHS Ch. 72 v. 30–31, page-image checked, treats Ashtakavarga as paramount over a transit check, so the two systems must not be merged). Interpretation of any transit fact is Phase 12, 15 and 17.
- **TR-15 Versions.** Phase 8 results record `standards_version = 1.6.0`. The Phase 5 Kundli constant stays `1.3.0`; Phase 7 results keep `1.5.0`.

### Ashtakavarga evidence-bundle integration (Phase 9 WP-EB methodology lock, v1.9.0)

- **AV-14 Facts contract.** `astro-engine` calculates and owns `AshtakavargaFacts` (WP-A1: profile, per-planet Bhinnashtakavarga charts with per-sign counts and per-contributor marks, Sarvashtakavarga, an optional Lagna chart) and, separately, `AshtakavargaReductionFacts` (WP-A2/A3: per-chart Trikona/Ekadhipatya Shodhana results and Rasi/Graha/Yoga Pinda). The rule engine never calculates Ashtakavarga: it records the JSON form of both (the second is optional and only meaningful together with the first, for the same profile and natal chart) in the evidence bundle as one additive optional `ashtakavarga` section (the bundle serializes and hashes exactly as before when absent). No shipped rule reads Ashtakavarga facts yet.
- **AV-15 Profile selection, no implicit default.** Unlike Dasha and Transit (one locked profile each), Ashtakavarga has four profiles (AV-02) with no default. The evidence-bundle integration does not choose one: the caller selects exactly one profile per bundle build and supplies its own already-computed `AshtakavargaFacts` (which itself carries `profile_id`, preserved verbatim on `AshtakavargaEvidence.profile.profile_id`). A caller wanting more than one profile's evidence builds more than one bundle. The static 56-cell cross-source conflict register (`CROSS_TABLE_CONFLICTS`, AV-04) is build-time registry data, not a per-Kundli fact, and is never copied into a bundle.
- **AV-16 Conflicts preserved verbatim.** Every `NOT_EVALUABLE` status and reason code from WP-A1/A2/A3 (`lagna_unavailable`, `natal_positions_incomplete`, `mercury_multiplier_conflict`, `multiple_occupants_unsupported`, `ekadhipatya_equal_value_conflict`) is copied unchanged into the evidence record; the two `EkadhipatyaConflict` readings (AV-10) are preserved with their full page-level provenance, never merged or resolved by the adapter. A chart reduction's map, and a chart's Rasi/Graha/Yoga Pinda, are copied only when the astro-engine contract itself marks them resolved -- the adapter enforces the same all-or-nothing contract astro-engine does (a malformed "partial success" cannot be constructed).
- **AV-17 Transport-only boundary.** The adapter (`pandit_rule_engine.ashtakavarga_evidence`) performs no astrology computation; it only re-shapes and validates already-computed JSON, exactly like the Dasha and Transit adapters. It does not import `astro-engine` (enforced by the existing repository-wide source-purity test, which globs every rule-engine source file).
- **AV-18 Versions.** This integration records no new `standards_version` of its own (each system's facts already carry their own, e.g. `ASHTAKAVARGA_STANDARDS_VERSION`); the rule-engine package version is bumped instead, as with the Dasha and Transit integrations.

## Jaimini standards (Phase 9 WP-B-1/WP-B-2 methodology lock, v1.10.0/v1.11.0)

Defines the standard and contract for **Phase 9 WP-B** (Jaimini systems), scoped to **WP-B-1 (Rashi Drishti) and WP-B-2 (Chara Karaka, Constant Karaka)**, extended by **WP-G** (v1.17.0, JN-11 to JN-18: planet-level Rashi Drishti, Arudha Pada, Karakamsa). Jaimini Dashas and every other Jaimini system remain unimplemented.

- **JN-01 Rashi Drishti, one profile, no default needed.** BPHS Ch. 8 v. 1-3 (Santhanam translation, `SRC-BPHS-SANTHANAM-1984`, Vol I, printed pp. 105-107, page-image verified) gives a static, longitude-independent sign-to-sign aspect rule: every movable sign aspects the 3 fixed signs other than the fixed sign adjacent to it; every fixed sign aspects the 3 movable signs other than the movable sign adjacent to it; every common (dual) sign aspects the other 3 common signs. Unlike Ashtakavarga, only one source was found for this rule (no cross-source variant), so a single profile, `RASHI_DRISHTI_BPHS_8_1_3`, is sufficient and no default-selection question arises. The relation is always mutual (proven by test, not assumed): if sign A aspects sign B, sign B also aspects sign A.
- **JN-02 Provenance: Parasara's rule, popularly nicknamed "Jaimini".** BPHS's own translator note (page-image verified, printed pp. 105-106) states this Rasi-aspect system is Maharshi Parasara's own, used also by sage Jaimini, and that it "came to be known as [the] Jaimini system though the original propounder is Parasara." The implementation and its documentation therefore never present this rule as Jaimini-authored; the profile ID and every citation name BPHS Ch. 8, not a Jaimini source.
- **JN-03 Kept strictly separate from graha drishti.** Rashi Drishti (sign-to-sign, `pandit_astro_engine.jaimini.rashi_drishti`) is never blended with, substituted for, or derived from Phase 5/6's Vedic graha drishti (planet-to-house, `aspects.py`, the universal 7th plus Mars/Jupiter/Saturn's special aspects). The two systems answer different questions (which signs aspect which signs, versus which houses a specific planet aspects from its placement) and a rule needing one must never silently receive the other's result.
- **JN-04 BPHS Ch. 8 v. 4-5 read but deliberately not implemented.** The same chapter extends the identical movable/fixed/common table to a planet's own placement ("the planet in a sign lends the same aspect as the sign... does"). This would produce a planet-level aspect that disagrees with the already-locked Vedic graha drishti for the same placement (JN-03), so it is recorded as read and page-image verified but is not implemented by WP-B-1; whether a future work package exposes it as a separate planet-level fact, clearly labelled and never merged with graha drishti, is an open item for the project owner.
- **JN-05 Versions.** This lock records no new `standards_version` of its own; astro-engine's existing Phase 9 versioning is unaffected (Rashi Drishti carries no chart-derived facts, so there is no `RashiDrishtiFacts.metadata` to version).
- **JN-06 Chara Karaka: two body-scope profiles, no default, source disagreement not silently resolved.** BPHS Ch. 32 v. 1-2 (Santhanam translation, page-image re-verified, printed p. 316) states three positions on which planets are Chara Karaka candidates, and does not choose one: the base rule (7 classical planets, Sun through Saturn); "some say" Rahu is a candidate only conditionally ("when there is a state of similarity in ... longitude between two planets", a condition the source does not define precisely enough to implement); "yet some say" 8 planets including Rahu must always be considered. Mirroring the Ashtakavarga precedent of never merging disagreeing readings, two profiles are shipped with no default -- `JAIMINI_CHARA_KARAKA_SEVEN_BODY_BPHS_32_1_17` and `JAIMINI_CHARA_KARAKA_EIGHT_BODY_BPHS_32_1_17` -- and the conditional third reading is recorded but not implemented. The chapter's own translator's note confirms its own worked example ("the standard nativity", Ch. 29) uses the 8-body reading. Ketu is never a candidate under any of the three positions.
- **JN-07 Ranking rule and Rahu's reverse degree.** Candidates are ranked by degree traversed within their own sign (v. 3-8: "whichever has traversed maximum number of degrees in a particular sign"), with Rahu's degree reversed (30 minus his degree in the sign) before comparison. Ties are resolved by minutes, then seconds, of arc; the eight roles, in the source's own order, are Atma, Amatya, Bhratru, Matru, Pitru, Putra, Gnati and Dara Karaka.
- **JN-08 Tie handling matches the source's own two-tier treatment, not a single uniform rule.** A tie "identical to the second of arc" (v. 13) between two candidates below the top rank makes both "qualified for that particular karakaatwa" (the source's own words) -- they share that role, and because roles fill strictly in rank order, the resulting shortfall always lands on the *lowest* role of the profile's sequence, reported as `NOT_EVALUABLE(rank_deficit)`. This is the same mechanism that gives the 7-body profile a structural Dara Karaka deficit even with no tie at all (7 candidates cannot fill 8 roles). A tie at the very top (Atma Karaka itself) is treated more strongly: because every other Karaka is judged relative to Atma Karaka (v. 9-12, "just as the minister cannot go against the king"), an unresolved top tie makes the *whole* result `NOT_EVALUABLE(tie_unresolved)`, with no partial role list -- this differentiated top-versus-other treatment is a Pandit Ji reading bridging v. 3-8 and v. 13, not a single verse's explicit statement, and is recorded as such rather than presented as directly verse-stated. The source's own "only seven significators, merging Matru and Putra" alternative (mentioned once, with no worked table) is read but not implemented, to avoid inventing the merged role's exact rank position.
- **JN-09 Constant Karaka: a separate system, two slots left undefined, no automatic substitution.** BPHS Ch. 32 v. 18-21 gives a static, longitude-independent planet-to-relative table (Mercury: siblings; Mars: maternal relatives; Jupiter: paternal grandfather; Venus: husband; Saturn: sons; Ketu: wife/father/mother/parents-in-law/maternal grandfather), plus two slots naming "the stronger" of a pair of planets (Sun-or-Venus for father, Moon-or-Mars for mother) without defining "stronger" anywhere in the chapter -- reproducing a conflict already on record in `research/ASTROLOGY_SOURCES.md` ("Ch. 32 v. 18-21 ... versus Ch. 3 ... with 'stronger' undefined"), so both slots are `NOT_EVALUABLE(strength_undefined)`. BPHS's own v. 13 illustration links a Chara Karaka deficit to this table (a tied Dara Karaka substitutes Venus), but only that one case is demonstrated; generalizing it into an automatic substitute for the other six non-Atma roles would invent a mechanism the source does not fully specify, so Constant Karaka is exposed as its own independent fact set, never auto-wired into Chara Karaka's output. Ch. 32 v. 22-24 ("Houses Related", reading each significator's effects from a counted house) is interpretive, not a fact, and is out of scope under the project's facts-only boundary.
- **JN-10 Versions.** v1.11.0 records no new `standards_version` of its own, for the same reason as JN-05 (Chara Karaka and Constant Karaka carry no chart-derived, versioned facts object yet).

- **JN-11 Planet-level Rashi Drishti (v1.17.0, WP-G).** `SOURCE_SUPPORTED` (BPHS Ch. 8 v. 4-5, Santhanam, printed p. 107, page image checked this phase): a planet casts the Rashi Drishti of the sign it occupies, on those signs and "simultaneously" on the planets in them. Profile `RASHI_DRISHTI_PLANET_BPHS_8_4_5` (`jaimini.planet_rashi_drishti`), which supersedes JN-04's "not implemented" for this separate profile only. The translator's worked example (a)-(f) agrees with the verse except (b), which says Venus and the Sun in Taurus aspect none although Taurus and Cancer (Jupiter) aspect each other and the example's own (c) has Jupiter aspecting them; the verse is followed. The example treats the nodes as aspecting planets; which bodies to pass is the caller's explicit choice.
- **JN-12 Still never blended.** JN-03 applies unchanged: planet-level Rashi Drishti is a separate fact set and is never merged with, substituted for, or used to answer a graha-drishti question (a test shows the two differ for the same placement).
- **JN-13 Bhava Pada (Arudha).** `SOURCE_SUPPORTED` (Ch. 29 v. 1-5, worked chart p. 295 and v. 4-5 p. 296 page-image checked): count the signs from a house to its lord inclusively, and as many again from the lord; if that lands on the house itself the Pada is the 10th from it, if on the 7th the Pada is the 4th from the house. Profile `ARUDHA_BHAVA_PADA_BPHS_29_1_5` (`jaimini.arudha.bhava_padas`); every result records which rule fired. House lords from the locked Phase 5 lordship table; the translator's nodal co-lordship of Scorpio and Aquarius (a note) is not used.
- **JN-14 Arudha source conflicts, recorded.** The translator's worked Arudha chart for the "standard nativity" (p. 295) agrees with the verse rule for houses 1-8, 11 and 12, but leaves the 9th and 10th Padas in the 7th house without the exception it applies to the 6th; the note's example "Aquarius Lagna, Saturn in Leo: Taurus" also contradicts the rule (Scorpio). The verse is followed, as elsewhere where a note or example contradicts a verse. The printed Sanskrit of v. 5, read by a non-qualified reader, agrees with the rule (unreviewed, `OCR-SOURCE-LANGUAGE` evidence only).
- **JN-15 Graha Pada.** `SOURCE_SUPPORTED` (Ch. 29 v. 6-7, OCR): count from the planet to its own sign and as many again. Only the Sun and Moon own one sign; the verse's "consider the stronger" for two-sign planets is undefined, so Mars to Saturn are `NOT_EVALUABLE(stronger_sign_undefined)`; the nodes own no sign in the locked table (`NOT_EVALUABLE(node_owns_no_sign)`). Profile `ARUDHA_GRAHA_PADA_BPHS_29_6_7`.
- **JN-16 Karakamsa.** `SOURCE_SUPPORTED` (Ch. 33 v. 1-2 with the translator's note, OCR): the Navamsa sign (Phase 5 D9) of the Atma Karaka taken from a Chara Karaka result computed under the caller's own profile (no default). Profile `KARAKAMSA_BPHS_33_1_2` (`jaimini.karakamsa`). The standard nativity's printed Navamsa chart (p. 294, page image) places the Moon, its Atma Karaka, in Sagittarius, as computed, and matches the Phase 5 D9 for all nine bodies.
- **JN-17 Karakamsa not evaluable.** When the Chara Karaka result has no Atma Karaka (a top tie, JN-08), Karakamsa is `NOT_EVALUABLE(atma_karaka_unresolved)`; it is never taken from the other profile.
- **JN-18 Versions and scope.** v1.17.0 records no `standards_version` field (pure functions, as JN-05). Ch. 29 v. 8 onward, Ch. 30-31 and Ch. 33 v. 2 onward (effects) and Jaimini Dashas are not implemented. Observation, not a change: the standard nativity longitudes printed on p. 294 give the Sun 37°12′18″, Mars 96°18′46″ and Saturn 63°09′41″, while the WP-B-2 fixture, transcribed from the Ch. 32 table (p. 319), gives the Sun 6°18′46″ and Mars 3°09′41″ (degrees within the sign); the Chara Karaka ranking is identical either way, and the frozen WP-B-2 test was not edited.

- **JN-19 WP-G facts object (v1.21.0).** `pandit_astro_engine.jaimini.facts.JaiminiFactsService` wraps planet-level Rashi Drishti, Bhava and Graha Padas, Karakamsa and the Chara Karaka ranking over one Phase 5 Kundli (Vedic default) into a `JaiminiFacts` object with `standards_version = 1.21.0` and one provenance entry per profile used. `chara_karaka_profile_id` and `include_nodes_in_rashi_drishti` have no default. The pure functions and their results are unchanged.
- **JN-20 Scope unchanged.** No rule reads these facts; Jaimini Dashas remain unimplemented (P9-07, a deferral accepted by the owner).

## Partial/Degree Drishti standards (Phase 9 WP-C methodology lock, v1.12.0)

Defines the standard and contract for **Phase 9 WP-C**, a continuous degree-based refinement of planet-to-house aspect strength, layered on top of the already-locked Phase 5 discrete house-offset aspect table (`aspects.py`). **WP-C is not a separately-named line item in `Phases.md`**: the roadmap lists only "Shadbala — BPHS Ch. 27-28" as the relevant Vedic strength/scoring module. BPHS Ch. 26 (this lock's primary source) is Ch. 27's own stated prerequisite -- Ch. 27 v.19 ("Drik Bala") explicitly aggregates a "Drishti Pinda" built from Ch. 26's per-pair values -- so this lock implements prerequisite groundwork for that roadmap item, not an independently-listed one. This is authorized by the project owner's explicit, extensively-researched decision (the highest-authority source under `SOURCE_OF_TRUTH.md`'s own authority order, above `Phases.md` itself), not by a `Phases.md` line item naming "Ch. 26" or "partial drishti" directly. **This lock does not implement Shadbala itself, Drik Bala's aggregation step, or any other Phase 9 system.**

- **PD-01 Two profiles, no default, never merged.** No cross-source variant of the discrete quarter/half/three-quarter/full progression was found (Brihat Jataka's and Phaladeepika's superficially similar chapters were checked and ruled out -- interpretive content, no comparable formula; Saravali returned a negative keyword result, inconclusive; Uttara Kalamrita was checked and found genuinely comparable). Two structurally different calculation methods for the *continuous* refinement were found and are shipped as two separate, explicitly-selected profiles, mirroring the Ashtakavarga precedent of never merging distinct traditions:
  - `PARTIAL_DEGREE_DRISHTI_BPHS_26` -- BPHS Ch. 26, Santhanam translation (`SRC-BPHS-SANTHANAM-1984`), Vol I pp. 254-255, Sanskrit-cross-checked (`sanskritdocuments.org` `par2130.html`, v.1-13).
  - `PARTIAL_DEGREE_DRISHTI_UTTARAKALAMRITA_SRIPATI` -- Uttara Kalamrita Ch. 2, Sastri translation (`SRC-UTTARA-KALAMRITA-SASTRI`), Sl.17½-19½, which itself cites "Sripatipaddhati-II" for the interpolation method.
- **PD-02 Shared discrete checkpoint table.** Both profiles independently state, and agree exactly on, the value at each house-start angle (0° increments of 30°): house 1 = 0, house 2 = 0, house 3 = 15, house 4 = 45, house 5 = 30, house 6 = 0, house 7 = 60 (full, universal), house 8 = 45, house 9 = 30, house 10 = 15, house 11 = 0, house 12 = 0. Houses 3/10, 4/8 and 5/9 share a value by BPHS's and Uttara Kalamrita's own explicit pairing statements; houses 1, 2, 6, 11 and 12 are never aspected by anyone, a resolved fact (`SUCCESS`, value 0), never `NOT_EVALUABLE`.
- **PD-03 Profile A formula.** `Δ = (aspected_longitude − aspecting_longitude) mod 360°`. For `Δ ∈ [0°,180°]`, BPHS's own five-branch formula (v.6-9, Sanskrit-verified): `(Δ−30°)/2` for `(30°,60°]`; `15+(Δ−60°)` for `(60°,90°]`; `30+(120°−Δ)/2` for `(90°,120°]`; `150°−Δ` for `(120°,150°]`; `(Δ−150°)×2` for `(150°,180°]`. For `Δ ∈ (180°,300°]`, BPHS's own literal reduction `Δ′ = 300°−Δ` (Sanskrit-confirmed: the verse's numeral is the bhūtasaṃkhyā word "dik" = 10, not a translation artifact -- see `research/ASTROLOGY_SOURCES.md` Group 14), then re-apply the same five branches to `Δ′`. For `Δ ∈ (300°,360°)`, value is 0 directly (houses 11-12, independently known, formula not needed there).
- **PD-04 Project-level mathematical derivation, not a source claim.** Each of PD-03's five branches is algebraically identical to linear interpolation between PD-02's adjacent checkpoint values. **This equivalence was derived by this project, mechanically verifiable, and is never to be presented as a statement made by BPHS or by Uttara Kalamrita themselves** -- it explains why the two profiles agree everywhere they overlap, without implying either source states the other's method.
- **PD-05 House 8 resolved; houses 9 and 10 are a genuine, permanent, unresolved conflict.** Applying PD-03's reduction literally: house 8 (`Δ=210°`) correctly reproduces the house-4 checkpoint (45) -- **`SUCCESS`-capable, not a conflict**. Houses 9 and 10 (`Δ=240°`, `Δ=270°`) do **not** reproduce their PD-02-stated checkpoints (giving 15 and 0 instead of 30 and 15) -- a provable disagreement between BPHS's own v.6-9 formula (which equals PD-04's interpolation everywhere else) and BPHS's own v.2 pairing statement, not merely an ambiguous reduction step. `NOT_EVALUABLE(reduction_rule_conflict)` applies to houses 9 and 10 only, for non-special aspecting planets, permanently -- no winner is chosen, and no Sanskrit-review or future finding is expected to silently resolve this without a fresh owner decision.
- **PD-06 Profile B formula.** Linear interpolation between PD-02's checkpoint values, using the fractional degree position within the current house as the interpolation weight, applied uniformly around the full circle including houses 9 and 10 -- no reduction step, because Profile B reads its endpoints directly from the stated checkpoint table rather than deriving them via BPHS's disputed reduction. This is why Profile B has no gap at houses 9-10: it is a different derivation route arriving at PD-02's already-stated values, not a resolution of PD-05's conflict.
- **PD-07 Special-planet handling, both profiles: conservative, no invented tolerance or interaction.** Saturn (houses 3, 10), Mars (houses 4, 8) and Jupiter (houses 5, 9) each have their own BPHS verse (v.9-12) confirming a value of exactly 60 (full) at the precise peak angle of their own special house -- `SOURCE_SUPPORTED`, confirmed independently for all three planets. Beyond that exact point, neither source specifies how the peak interacts with the surrounding continuous curve.
  - **Profile A**: given the exact literal peak `Δ`, returns `SUCCESS`, value 60 (exercises and documents the genuinely source-supported fact via a dedicated test, not a runtime tolerance). Any other `Δ` within that planet's own special house returns `NOT_EVALUABLE(special_formula_interior_unresolved)`. No rounding or "close enough" comparison exists anywhere in this profile.
  - **Profile B**: any query where the aspecting planet is Saturn/Mars/Jupiter and the house is that planet's own special house returns `NOT_EVALUABLE(special_formula_interior_unresolved)` **unconditionally, including at the exact peak angle** -- Uttara Kalamrita's own interpolation method was never stated as applying to the special-planet case at all, so no exact-angle carve-out is manufactured for Profile B the way PD-07's Profile A rule allows.
  - For any house a special planet does *not* own (e.g. Mars aspecting the 3rd house), that planet uses the ordinary PD-03/PD-06 formula exactly like any other planet -- special handling is scoped strictly to (owning planet, own house) pairs.
- **PD-08 Provenance and unresolved translation-level uncertainty, preserved verbatim.** Profile B's attribution to "Sripatipaddhati-II" is `TRANSLATION-LEVEL`, Uttara Kalamrita's own citation, not independently verified -- the primary Sripatipaddhati text was unreachable via two independent technical attempts (a non-functional archive.org reader/search-index and a failed direct-file fetch), not a content-based finding; every result computed under this profile must carry a provenance note stating the attribution is unverified. Separately, one interior operation of BPHS's own Saturn-specific verse (v.9-10) is read two different ways -- the published English translation says "multiply the degrees by 2," an unreviewed reading of the Sanskrit by this project says "divide by 2" -- and this discrepancy is not resolved; it is fully subsumed within PD-07's `special_formula_interior_unresolved` handling, so no separate runtime behavior depends on picking one reading over the other.
- **PD-09 Reason codes.** `reduction_rule_conflict` (PD-05, Profile A houses 9-10 only), `special_formula_interior_unresolved` (PD-07, both profiles' special-planet own-house cases) and `aspecting_node_unresolved` (PD-10, both profiles, Rahu/Ketu as the aspecting body) are the three reason codes this lock defines; none is a vague umbrella for another, and no other reason code stands in for any of them.
- **PD-10 Rahu/Ketu as the aspecting body: genuine source silence, resolved conservatively, symmetric across both profiles.** Neither BPHS Ch. 26 (v.1-13, fully read) nor Uttara Kalamrita Ch. 2 (Sl.17½-19½, fully read) names Rahu or Ketu anywhere, positively or negatively, as a body that casts this kind of aspect -- a genuine source silence in both texts, not a basis for either inclusion or exclusion. BPHS Ch. 27 ("Evaluation Of Strengths," the very next chapter, which consumes Ch. 26's values for its Drik Bala component) does explicitly state "these strengths are computed for the seven Grahas from Sūrya to Śani. The nodes are not considered" -- but this is a statement about **whose Shadbala score gets computed** (a different chapter, a different purpose: a per-planet aggregate strength total), not an explicit statement about which bodies may **cast** a Drishti under Ch. 26's own definition, and it is **not extended into a Ch. 26 rule** by this lock. Existing decisions from other, unrelated systems (Phase 5's graha drishti, which includes Rahu/Ketu at the universal 7th only; Ashtakavarga, which never uses either node as a contributor; Chara Karaka, which includes Rahu conditionally and excludes Ketu definitively) are each that system's own explicit, separately-sourced decision and are **not imported into WP-C**.
  - `CelestialBody.RAHU`/`.KETU` remain structurally valid `aspecting_body` values in both profiles -- this is a business-rule outcome, not a request-validation rejection (unlike Chara Karaka's definitive, source-stated Ketu exclusion, which *is* a validation error; this is a genuine unresolved question, which is a computed result, not a malformed request).
  - When `aspecting_body` is `RAHU` or `KETU`, both profiles return `NOT_EVALUABLE(aspecting_node_unresolved)`, symmetrically -- no Drishti value is calculated or invented, and the two profiles must never apply different rules to this case. The raw separation and derived house are still preserved in the result wherever the result model already carries them for other `NOT_EVALUABLE` cases, consistent with PD-05/PD-07's own transparency.
- **PD-11 Versions.** This lock records no new `standards_version` of its own; astro-engine's existing Phase 9 versioning is unaffected (no chart-derived, versioned facts object exists here yet, matching AV-18/JN-05/JN-10's precedent).

## Western standards (Phase 9 WP-D methodology lock, v1.14.0)

Defines the standard and contract for **Phase 9 WP-D**, the Western astrology module named in `Phases.md` Phase 9 ("Western Astrology", "Tropical zodiac", "Western aspects", "Placidus") and `features.md` §4 and §9. It replaces the uncited Phase 1 "Western / Tropical" bullets above wherever they differ (see the note there). Each item carries one of the owner's classifications: `SOURCE_SUPPORTED` (read directly in the cited source at the stated level), `PROJECT_DERIVED` (a Pandit Ji engineering decision), `INFERENCE`, `UNRESOLVED_CONFLICT` or `NOT_EVALUABLE`. Sources are registered in `research/ASTROLOGY_SOURCES.md` Group 15; the profile IDs are listed in its §6.6. The project owner authorized this work package to run research, methodology lock and implementation in one execution, with the rule that where sources conflict a defensible engineering default is chosen, documented and the alternatives kept as profiles where practical; every such default below is labelled.

- **WD-01 Scope and separation.** `PROJECT_DERIVED`. Facts and provenance only: tropical positions, signs, Placidus cusps and angles, house placement, aspects with orbs and motion state. No interpretation, no personality or event text, no Vedic-versus-Western comparison (a later phase), no EvidenceBundle section (a separate approval gate, as for every earlier Phase 9 package). The module (`services/astro-engine/src/pandit_astro_engine/western/`) has its own body, sign and aspect identifiers, imports no Vedic module and no `swisseph`, and never uses graha drishti, Rashi Drishti, Partial/Degree Drishti, the ayanamsa or the Vedic node policy. Every result carries `system = "western_tropical"`.
- **WD-02 Tropical zodiac and signs.** `SOURCE_SUPPORTED` (Swiss Ephemeris documentation §2.8.1, read directly): 0° Aries is the vernal point; the tropical and sidereal zodiacs drift apart by precession. Profile `WESTERN_ZODIAC_TROPICAL_OF_DATE`: geocentric apparent ecliptic longitude of date as Swiss Ephemeris returns it without the sidereal flag (so no ayanamsa is ever applied). Signs are the twelve 30° segments from 0° Aries. `PROJECT_DERIVED`: intervals are half-open `[30k, 30k+30)`, 360° = 0°, classified with exact rational arithmetic on the float value (the v1.4.1 Nakshatra convention), so a longitude exactly on a boundary belongs to the later sign; non-finite longitudes are rejected.
- **WD-03 Body profiles.** No default is silently applied: the request field defaults to `WESTERN_BODIES_MODERN_10`, and the profile used is recorded in every result.
  - `WESTERN_BODIES_CLASSICAL_7` -- Sun, Moon, Mercury, Venus, Mars, Jupiter, Saturn. `SOURCE_SUPPORTED` (Lilly, *Christian Astrology* Book I, the seven planet chapters, `OCR-ORIGINAL-ENGLISH`).
  - `WESTERN_BODIES_MODERN_10` (engineering default) -- the seven plus Uranus, Neptune and Pluto. `PROJECT_DERIVED` default, labelled `modern_tradition`: the outer planets are modern discoveries with no classical source; they are included because present-day Western charts use them (a practice observation, not a source claim).
  - `WESTERN_BODIES_MODERN_10_NODES` -- the ten plus the lunar nodes (WD-05).
- **WD-04 Excluded bodies.** `NOT_EVALUABLE` / out of scope: Chiron and the asteroids need the Swiss Ephemeris main-asteroid files (`seas_18.se1`, §2.3.1), which are not bundled (licensing, `LEGAL_REGULATIONS.md`) -- Moshier mode cannot compute them; the documentation also states Chiron is only well computed for 700-4650 CE (§2.3.4). The Part of Fortune (day/night formula variants not researched), Black Moon Lilith, the Vertex and fixed stars are not researched. None may be added to a default profile without a new lock.
- **WD-05 Lunar nodes.** `PROJECT_DERIVED`. Positions only: the nodes take no part in aspects (no orb source was researched for them). The node convention (mean or true, the existing Phase 4 conventions) has **no default** and must be given explicitly when, and only when, the node profile is chosen; it is recorded in the result. The south node is the north node plus 180°, with the latitude negated and the same speed.
- **WD-06 Placidus houses.** `SOURCE_SUPPORTED` (Swiss Ephemeris documentation §6.2.1, read directly): cusp 11 has completed 2/3 and cusp 12 1/3 of its semi-diurnal arc; cusps 2 and 3 likewise on the semi-nocturnal arc; cusps 4-9 are the opposites. Profile `WESTERN_HOUSES_PLACIDUS_SWISSEPH`, computed by Swiss Ephemeris (`houses_ex`, `P`, tropical). Cusp 1 is the Ascendant and cusp 10 the MC; both are reported with their tropical signs. Placidus is the only house system offered; Whole Sign, Equal, Koch, Regiomontanus, Campanus, Porphyry and others are future profiles and are rejected as unknown IDs today.
- **WD-07 Polar and failure policy.** `SOURCE_SUPPORTED` (documentation §6.4 and §6.7): Placidus cannot always be computed beyond the polar circles, and Swiss Ephemeris then switches to Porphyry and warns; since SE 2.09 a non-converging iteration (at most 100 steps) also switches to Porphyry. `PROJECT_DERIVED`: the substitute is never accepted. The polar circle is taken as |latitude| ≥ 90° − the true obliquity of date (inclusive); this was checked to agree with Swiss Ephemeris's own switch to within 1e-9° at three dates, north and south. Inside it the houses are `NOT_EVALUABLE(placidus_polar_circle)`; any other Swiss Ephemeris house failure is `NOT_EVALUABLE(placidus_not_computable)` with the library's message. The Ascendant and MC are not reported in either case (the documentation notes the MC may lie below the horizon there). Positions and aspects are still evaluated, since they do not depend on houses.
- **WD-08 House placement.** `PROJECT_DERIVED`. A body is placed by ecliptic longitude only (the "traditional way" the Swiss Ephemeris documentation describes as setting the latitude to zero), in the half-open arc `[cusp n, cusp n+1)` in zodiacal order, house 12 running to cusp 1, using exact rational arithmetic; a body exactly on a cusp is in the house that cusp opens. Cusps must be twelve, strictly increasing in zodiacal order and closing the circle once, or the placement raises.
- **WD-09 Aspect set.** `WESTERN_ASPECTS_PTOLEMAIC_5`: conjunction (0°), sextile (60°), square (90°), trine (120°), opposition (180°). `SOURCE_SUPPORTED` for the four: Ptolemy, *Tetrabiblos* I.13 (Robbins, Loeb 1940, read in the LacusCurtius web transcription, `WEB-TRANSCRIPTION-TRANSLATION`, not page-image checked). `UNRESOLVED_CONFLICT`, recorded, not a blocker: Ptolemy does not class the conjunction as an aspect (Robbins's note says it is nevertheless treated as one throughout), while Lilly treats it as one; it is included, following Lilly. Ptolemy's aspects are between signs; the degree-and-orb method here follows Lilly. Minor aspects (Kepler's semisquare, quintile, sesquiquadrate and others; the quincunx) are not supported -- Robbins notes they are inconsistent with Ptolemy's doctrine -- and have no profile.
- **WD-10 Aspect geometry.** `PROJECT_DERIVED`. Separation is the shorter arc between the two longitudes, in [0°, 180°]; deviation is |separation − exact angle|; an aspect holds when deviation ≤ the allowed orb (inclusive), compared with exact rational arithmetic. Signs and houses are never inputs (no "out-of-sign" rule). Each unordered pair of aspect bodies is evaluated once, in the body profile's order, so no pair appears twice or mirrored. Every orb profile's largest orb is below half the smallest gap between aspect angles (15°), so at most one aspect can hold per pair; a test checks this for every profile.
- **WD-11 Orbs: no single standard.** `UNRESOLVED_CONFLICT`. Ptolemy gives no orb values at all (I.24 says only "when the interval between them is not great"; Robbins's note attributes a table -- Saturn 10°, Jupiter 12°, Mars 7°30′, Sun 17°, Venus 8°, Mercury 7°30′, Moon 12°30′ -- to Ashmand's reading and a 15° maximum to an anonymous commentator, so that table must never be called Ptolemaic). Lilly gives per-planet orbs that disagree with his own worked example (WD-13). Modern practice varies. Two explicit orb profiles are shipped; the choice is recorded in every result.
- **WD-12 Fixed-orb default.** `WESTERN_ORB_FIXED_V1`, `PROJECT_DERIVED` (`engineering_convention`), the request default: 8° for conjunction, square, trine and opposition, 6° for sextile, the same for every body, including the outer planets. Chosen because it covers the default modern body set (Lilly's orbs cannot) and stays well inside the 15° uniqueness bound; it is not presented as a source rule.
- **WD-13 Lilly moiety profile.** `WESTERN_ORB_LILLY_1647_MOIETY`, `SOURCE_SUPPORTED` at `OCR-ORIGINAL-ENGLISH` level: per-planet orbs from Lilly's planet chapters -- Saturn 9°, Jupiter 9°, Mars 7°, Sun 15°, Venus 7°, Mercury 7°, Moon 12° (1647 text; Mars read from the 1659 text only) -- combined by moiety: a pair is in orb when the deviation is at most half of each planet's orb, added (Lilly's platick-aspect and separation rule). The same allowance applies to every aspect. Pairs involving Uranus, Neptune or Pluto are `NOT_EVALUABLE(orb_not_defined_for_body)` and listed as such. `UNRESOLVED_CONFLICT` preserved, not implemented: Lilly's own platick example uses moieties Saturn 5° and Venus 4° (orbs 10° and 8°), agreeing with neither his planet chapters nor, necessarily, the table he prints after it, which is illegible in the OCR read; a table-based Lilly profile needs page-image verification first.
- **WD-14 Applying and separating.** `WESTERN_MOTION_INSTANTANEOUS_V1`, `PROJECT_DERIVED` with an `INFERENCE`: `applying` when the deviation is decreasing at the chart instant, from the two bodies' signed longitudinal speeds; `separating` when increasing; `exact` when the deviation is exactly zero; `NOT_EVALUABLE(relative_motion_zero)` when the relative speed is exactly zero. Lilly's three ways of application (the swifter planet approaching the slower, both direct; both retrograde; one retrograde) all describe a closing distance, and reading them as the sign of this rate is a Pandit Ji inference. Retrograde means speed < 0 (the Phase 4 semantics).
- **WD-15 Birth-time precision and missing data.** `PROJECT_DERIVED`. `time_precision` is required (`exact` or `unknown`, no default). With `unknown`, the supplied clock time is a placeholder: positions at that instant are reported with a warning, and the houses, angles, house placement and aspects are all `NOT_EVALUABLE(birth_time_unknown)` (the Moon alone moves about 13° a day, more than any orb). No birth-time rectification.
- **WD-16 Input validation.** `PROJECT_DERIVED`. The existing Phase 4 models and errors are reused: latitude in [−90, 90], longitude in [−180, 180], IANA timezones, strict DST handling unless a disambiguation policy is given. Unknown profile IDs, extra fields, a missing time precision, a node convention without the node profile, or the node profile without a node convention are rejected at request validation. UTC is canonical; the Julian day is derived with the Phase 4 conversion.
- **WD-17 Defaults.** `PROJECT_DERIVED`, each recorded in every result: zodiac `WESTERN_ZODIAC_TROPICAL_OF_DATE` (the only one), bodies `WESTERN_BODIES_MODERN_10`, houses `WESTERN_HOUSES_PLACIDUS_SWISSEPH` (the only one), aspects `WESTERN_ASPECTS_PTOLEMAIC_5` (the only one), orbs `WESTERN_ORB_FIXED_V1`, motion `WESTERN_MOTION_INSTANTANEOUS_V1` (the only one). No default for the node convention or the time precision. Nothing falls back from one profile to another.
- **WD-18 Provenance and warnings.** Every result carries a provenance entry for the zodiac, bodies, houses, aspects, orbs, motion and ephemeris accuracy, each with its evidence label and source references, plus warnings for Moshier mode, an unknown birth time and the derived south node.
- **WD-19 Versions.** Western results record `standards_version = 1.14.0`. astro-engine advances to 0.8.0. No earlier result's recorded version changes.
- **WD-20 Validation evidence and limits.** `ENGINEERING_EVIDENCE`: Swiss Ephemeris Moshier tropical longitudes agree with NASA/JPL Horizons within 0.63″ for Uranus, Neptune and Pluto on 12 samples (1950-2026, fixture `western_outer_planets_horizons.json`, retrieved 2026-09-24) and within the Phase 8 figures for the seven classical bodies; Placidus cusps agree with an independent implementation of the §6.2.1 definition (using only Swiss Ephemeris's ARMC and true obliquity) to better than 0.01″ over 250 random charts. Limitations: Moshier mode unless data files are configured; UT1-UTC is not modelled; the Lilly values are `OCR-ORIGINAL-ENGLISH` with no page-image check, and the Tetrabiblos was read in a web transcription only; no Western source is verified in its original language; the Ashmand table, a table-based Lilly profile, other house systems, minor aspects, Chiron, the Part of Fortune and node aspects are future work.

## KP standards (Phase 9 WP-E methodology lock, v1.15.0)

Defines the standard and contract for **Phase 9 WP-E**, the KP foundation behind `Phases.md` Phase 9 "KP Astrology" and "KP Horary". Classifications as in §Western standards. Sources: `research/ASTROLOGY_SOURCES.md` Group 16 (K. S. Krishnamurti, KP Readers I, III and VI, read in archive.org OCR text, `OCR-ORIGINAL-ENGLISH`; Swiss Ephemeris documentation §2.8.6, `DOCUMENTATION-DIRECT`). **This is a foundation, not complete KP**: no event judgment, no timing of events, no conjunction or aspect significators, no node agency.

- **KP-01 Scope and separation.** `PROJECT_DERIVED`. Facts and provenance only, in `services/astro-engine/src/pandit_astro_engine/kp/`: KP positions, cusps, star/sub/sub-sub lords, the 249-entry table, house significators, Ruling Planets and the horary chart. System tag `kp_krishnamurti_paddhati`. KP shares only the Phase 4 adapter, time resolution, and the sign, Nakshatra and Vimshottari identifiers the KP Readers themselves use; it applies no Parashari yoga, dosha, dignity or drishti rule, and no Western rule (a test scans the imports).
- **KP-02 Ayanamsa.** `SOURCE_SUPPORTED`: KP Reader I (pp. 56-59) gives a zero year of 291 CE, Newcomb's 50.2388475″/year and a yearly table. Default profile `KP_AYANAMSA_KRISHNAMURTI_SWISSEPH` = Swiss Ephemeris `SE_SIDM_KRISHNAMURTI` (mode 5), which the Swiss Ephemeris documentation says was fitted to that table; `ENGINEERING_EVIDENCE`: it agrees with the printed table within 0.9′ over 1880-2001 (the table is rounded to the arcminute). KP never uses the Vedic default's Lahiri ayanamsa. `PROJECT_DERIVED`: the Swiss Ephemeris sidereal mode is process-global, so the KP mode is applied only for the duration of a call and the previously applied mode is restored (`ephemeris.kp_sidereal_mode`); a test proves a Vedic Kundli is identical before and after KP calls.
- **KP-03 Alternative ayanamsa.** `KP_AYANAMSA_KRISHNAMURTI_VP291_SWISSEPH` (mode 45, D. Senthilathiban's reading of the zero year as the 291 CE equinox) is selectable, never a default; it runs about 1′ higher than the Reader's table.
- **KP-04 Houses.** `KP_HOUSES_PLACIDUS_SIDEREAL_SWISSEPH`. `INFERENCE`: Reader I names Placidus's semi-arc system as the method in common use and prescribes Raphael's Tables of Houses; that Raphael's tables are Placidian is outside knowledge. `SOURCE_SUPPORTED` (Reader III): a house runs from its cusp to the next cusp and its lord is the lord of the sign holding the cusp. Cusps are Swiss Ephemeris Placidus with the sidereal flag; placement is half-open `[cusp n, cusp n+1)`. |latitude| ≥ 90° − true obliquity gives `NOT_EVALUABLE(placidus_polar_circle)`, any other failure `NOT_EVALUABLE(placidus_not_computable)`; the Porphyry substitute is never used (as WD-07).
- **KP-05 Star and sub lords.** `SOURCE_SUPPORTED` (Reader III pp. 10-13): each 13°20′ star is divided among the nine Vimshottari lords in proportion to their years (years × 400″), starting with the star lord, in dasha order; subs cut by a sign boundary count twice, giving 249 sign-star-sub entries. The table is derived, never transcribed. `ENGINEERING_EVIDENCE`: all 202 legible rows of the Reader III list agree exactly with the derived table (fixture `kp_reader3_sub_table.json`; 11 further OCR rows each differ by one misread digit), and the Reader VI examples (48: Gemini 8°40′, Rahu star, Jupiter sub; 74: Cancer 14°53′20″, Saturn star, Jupiter sub) are reproduced.
- **KP-06 Sub-sub lords.** `PROJECT_DERIVED` (derived calculation): the same proportional rule inside the sub. The Reader mentions further subdivision only "for research students".
- **KP-07 Boundaries.** `PROJECT_DERIVED`: exact rational arithmetic in arcseconds; half-open intervals, so an exact boundary belongs to the later star or sub; `near_boundary` within 1e-6°. Exact inputs (a table boundary used as a horary Ascendant) are classified from the exact value, not its float.
- **KP-08 Significators.** `KP_SIGNIFICATORS_FOUR_LEVEL_READER_VI`, `SOURCE_SUPPORTED` (Reader VI, order given for houses 2, 5, 11): (a) planets in the star of an occupant, (b) occupants, (c) planets in the star of the house lord, (d) the house lord; plus the inverse per-planet view.
- **KP-09 Not evaluated.** The Reader's further levels (planets conjoined with, or aspected by, significators) and the nodes' agency for other planets are listed as `not_evaluated` on every result: KP's conjunction and aspect conventions are not locked. Nodes are treated as occupants and star lords only.
- **KP-10 Ruling Planets.** `KP_RULING_PLANETS_READER_VI`, `SOURCE_SUPPORTED` (Reader VI p. 123): Ascendant star lord and sign lord, Moon star lord and sign lord, day lord; a node joins when it occupies a sign owned by the Ascendant sign lord, Moon sign lord or day lord. Members in the star of a retrograde planet are flagged, not removed (the Reader says "rejected"); mean nodes are always retrograde, so for a node star lord the flag is `node_retrogression_unresolved`. `UNRESOLVED_CONFLICT`: the Reader does not say where the day begins, so `day_lord_convention` (`local_civil_date` or `sunrise_to_sunrise`) has **no default**; with no sunrise the Ruling Planets are `NOT_EVALUABLE(sunrise_unavailable)`.
- **KP-11 Horary.** `KP_HORARY_NUMBER_249_READER_VI`, `SOURCE_SUPPORTED`: number 1-249 selects a table entry; its start is the Ascendant; the other cusps are the Placidus cusps for that Ascendant at the latitude of judgment ("all places in the same latitude will have the same cusps"); planets and Ruling Planets are for the moment of judgment. `PROJECT_DERIVED`: the ARMC is solved numerically with the true obliquity of the judgment date (the Reader reads printed tables), residual below 1e-7°. `ENGINEERING_EVIDENCE` (fixture `kp_reader6_horary_29.json`): the Reader's example 29 (Bombay, 6 May 1969, 17:30 IST) is reproduced for the 7th cusp (Scorpio 10°00′, Saturn star, Venus sub), the ayanamsa (23°20′), the Moon (21°22′) and Venus (Pisces 18°00′), and the printed dasa balance follows from the Moon; intermediate cusps differ by 4-9′ from the Reader's table values, and the resulting differences in the house 2 and 5 significator lists are recorded in the fixture.
- **KP-12 Unknown birth time.** `PROJECT_DERIVED`: positions are reported with a warning; cusps, all star/sub lordships and significators are `NOT_EVALUABLE(birth_time_unknown)`.
- **KP-13 Nodes.** The node convention (mean or true) is required with no default: the Readers do not fix it. Ketu is Rahu + 180°.
- **KP-14 Validation.** Existing Phase 4 models and typed errors; unknown profile IDs, a horary number outside 1-249 and missing required conventions are rejected.
- **KP-15 Versions.** KP results record `standards_version = 1.15.0`; astro-engine advances to 0.9.0 with the rest of Phase 9.
- **KP-16 Licensing and limits.** K. S. Krishnamurti died in 1972; the KP Readers are presumed in copyright in India until the end of 2032 and are cited, never reproduced (the fixtures hold only lords and degree ranges). Moshier mode unless data files are configured; OCR-level reading only; no Reader page image was checked.

## Shadbala standards (Phase 9 WP-F methodology lock, v1.16.0)

Defines the standard and contract for **Phase 9 WP-F**, `Phases.md` "Shadbala — BPHS Ch. 27-28". One profile, `SHADBALA_BPHS_SANTHANAM_27_VERSE`: every component follows the translated **verse** of BPHS Ch. 27 (`SRC-BPHS-SANTHANAM-1984`, Vol I printed pp. 264-288, all 40 verses re-read in OCR this phase, `OCR-TRANSLATION`). Where a component's method exists only in the translator's notes, or verse and notes disagree, or the verse is ambiguous, the component is `NOT_EVALUABLE` with a reason; nothing is filled from a note-only or modern method. **Stated plainly: no Shadbala Pinda (total) is produced today.**

- **SB-01 Scope.** `PROJECT_DERIVED`. Component facts for the seven planets Sun to Saturn (Ch. 27 v. 1 note: "The nodes are not considered"), under the Vedic default profile (sidereal, Lahiri, whole-sign houses) using the Phase 5 Kundli. System tag `vedic_parashari`. No "strong/weak" judgment.
- **SB-02 Units.** Virupas, unrounded floats (60 Virupas = 1 Rupa). Every component carries status (`success`, `not_evaluable`, `not_applicable`), reason and evidence label; the provenance lists every component with its locator.
- **SB-03 Uchcha Bala.** `SOURCE_SUPPORTED` (v. 1): (longitude − deep debilitation point) folded to ≤ 180°, ÷ 3; debilitation points from the Phase 5 dignity table. The translator's worked example (Sun Pisces 12°15′ → 50.75) is reproduced.
- **SB-04 Saptavargaja Bala.** `SOURCE_SUPPORTED` values (v. 2-4): Moolatrikona 45, own 30, great friend 20, friend 15, equal 10, enemy 4, great enemy 2, in D1, D2, D3, D7, D9, D12 and D30 (Phase 5 schemes). `TRANSLATOR_NOTE`: the compound relationship is taken from the Rasi chart (locked Phase 6 tables, drift-tested against the knowledge YAML). `UNRESOLVED_CONFLICT`: the verse says "Moolatrikona Rasi" (a sign) while Ch. 3 v. 51-54 gives a degree range; in the divisional charts (signs only) the Moolatrikona sign counts as Moolatrikona, but a D1 planet in its Moolatrikona sign outside the Ch. 3 range makes the component `NOT_EVALUABLE(d1_moolatrikona_sign_versus_degree)`.
- **SB-05 Ojayugma, Kendradi, Drekkana.** `SOURCE_SUPPORTED` (v. 4-6; genders Ch. 3 v. 19): 15 each for Rasi and Navamsa odd/even placement (Moon and Venus even, the others odd; the note's "35" contradicts the verse and is not used); angle 60, succedent 30, cadent 15 by whole-sign house from the Lagna (the locked house baseline, a `PROJECT_DERIVED` basis); 15 to a male, female or neuter planet in the first, second or third 10° of its sign.
- **SB-06 Dig Bala.** v. 7 formula (as SB-03) against the zero points: the 4th for the Sun and Mars, the 7th for Jupiter and Mercury, the 10th for Venus and the Moon, the Ascendant for Saturn. `TRANSLATOR_NOTE`: the translation glosses these as the Nadir, descendant and meridian, used as sidereal MC + 180°, Lagna + 180° and sidereal MC.
- **SB-07 Nathonnatha Bala.** `SOURCE_SUPPORTED` (v. 8-9): Unnata = apparent time from apparent midnight (0-30 ghatis); Moon, Mars, Saturn 2 × (30 − Unnata); Sun, Jupiter, Venus 60 minus that; Mercury always 60. `PROJECT_DERIVED`: apparent time from Swiss Ephemeris's equation of time. The note's "simple method" contradicts the verse and is not used.
- **SB-08 Paksha Bala.** `SOURCE_SUPPORTED` (v. 10-11): benefics get (Moon − Sun, folded ≤ 180°) ÷ 3, malefics 60 minus that; benefic/malefic by the locked Phase 6 natural-nature standard (waning Moon malefic; Mercury malefic when in the same sign as a malefic). `NOT_EVALUABLE(moon_phase_boundary)` at the phase boundary, `NOT_EVALUABLE(mercury_mixed_association)` for Mercury with both. The note's doubling of the Moon's value is not in the verse and is not applied.
- **SB-09 Tribhaga Bala.** `SOURCE_SUPPORTED` (v. 12): day thirds Mercury, Sun, Saturn; night thirds Moon, Venus, Mars; Jupiter always 60. Day = sunrise to sunset (Phase 4 rise/set); with no sunrise/sunset near the instant, `NOT_EVALUABLE(sunrise_unavailable)`.
- **SB-10 Vara Bala.** `SOURCE_SUPPORTED` (v. 13): 45 to the weekday lord, the day reckoned from sunrise (the note, and the locked Vara standard).
- **SB-11 Abda and Masa Bala.** `NOT_EVALUABLE(year_month_lord_method_unverified)`. The verse gives only the values (15, 30); the lords are found only in the note, via a Surya Siddhanta ahargana table whose stated divisor (160) disagrees with its own worked example (360).
- **SB-12 Hora Bala.** `NOT_EVALUABLE(hora_method_not_locked)`: equal hours from sunrise appear only in the note; no Hora standard is locked (Phase 10).
- **SB-13 Ayana Bala.** `NOT_EVALUABLE(ayana_method_conflict)`: the verse's khanda procedure (45, 33, 12) and the note's declination formula ((23°27′ ± kranti) × 1.2793, with the Sun doubled) are different methods.
- **SB-14 Yuddha Bala.** `NOT_EVALUABLE(planetary_war_undefined)` for Mars to Saturn (v. 20 does not define war and adjusts finished totals); `not_applicable` for the Sun and Moon.
- **SB-15 Cheshta Bala.** The Moon's is her Paksha Bala (v. 18, evaluated); the Sun's is his Ayana Bala (not evaluable); Mars to Saturn `NOT_EVALUABLE(cheshta_method_conflict)`: v. 21-23 (eight motions, values partly illegible) and v. 24-25 (Cheshta Kendra from the mean longitude and the sighrocca, elements not supplied) are two methods.
- **SB-16 Naisargika Bala.** `SOURCE_SUPPORTED` (v. 14): 60/7 × 1 (Saturn) to 7 (Sun).
- **SB-17 Drik Bala and totals.** Drik is `NOT_EVALUABLE(drik_formula_ambiguous)`: v. 19 does not fix one arithmetic and its Ch. 26 inputs are themselves not evaluable in some cases. Sthana, Kala and Shadbala totals are produced only when every part is evaluated; otherwise `NOT_EVALUABLE(component_not_evaluable)` naming the missing parts. `evaluated_subtotal_virupas` is the sum of the evaluated leaf components and is labelled partial, never a Shadbala.
- **SB-18 Unknown time and inputs.** With `time_precision = unknown`, everything except Naisargika is `NOT_EVALUABLE(birth_time_unknown)`. The node convention (Vedic default mean) matters only for Mercury's association.
- **SB-19 Versions.** Shadbala results record `standards_version = 1.16.0`.
- **SB-20 Deferred.** *(v1.21.0: the missing components and a total are now available only under the separate modern profile below, SR-01 to SR-24; this BPHS verse profile is unchanged.)* Ch. 27 v. 26-31 (Bhava Bala), v. 32-36 (minimum requirements, which need totals) and Ch. 28 (Ishta/Kashta; its rays arithmetic is ambiguous in this translation and needs the Cheshta Kendra) are research-only. No Ch. 27 page image was checked this phase.

### Modern profile: B. V. Raman, Graha and Bhava Balas (v1.21.0, SR-01 to SR-24)

The project owner reopened Shadbala on 2026-09-25 and asked for a separately labelled, evidence-backed modern methodology for the components and total that the BPHS verse profile cannot evaluate. This lock adds that profile. It does **not** change the BPHS verse profile (SB-01 to SB-20), which still produces no total. Source: B. V. Raman, *Graha and Bhava Balas*, second edition (`SRC-RAMAN-GRAHA-BHAVA-BALAS`, `research/ASTROLOGY_SOURCES.md` Group 22), read in the archive.org OCR and checked against page images of the worked Standard Horoscope and Tables IV-IX (`IMAGE-ORIGINAL-ENGLISH`). Raman says he follows Sripati. Every rule below is labelled `modern_tradition` unless stated.

- **SR-01 Separation.** `SHADBALA_RAMAN_GRAHA_BHAVA_BALAS` has its own request (`RamanShadbalaRequest`), service (`RamanShadbalaService`), component table and provenance. The BPHS service never calls it and it never returns a BPHS-profile value. Where Raman's rule is arithmetically the same as a BPHS verse rule (Uchcha, Ojayugma, Kendra, Dig, Nathonnatha, Tribhaga, Naisargika), the same helper is used and the provenance cites Raman. A caller that wants both profiles makes two calls.
- **SR-02 Evidence.** Each component was checked by feeding Raman's own printed inputs for his Standard Horoscope (16 October 1918, 2 p.m. LMT, 13 N, 77 E 35) into the formula and comparing with his printed result (fixture `raman_standard_horoscope.json`, 68 tests). Where his printed figure disagrees with his own stated rule, the rule is implemented and the slip recorded (SR-24).
- **SR-03 Positions.** Positions, Lagna and vargas come from the Phase 5 Kundli under the Vedic default (sidereal Lahiri, whole-sign houses), as for the BPHS profile. `PROJECT_DERIVED`.
- **SR-04 Uchcha Bala.** As SB-03 (Art. 20).
- **SR-05 Saptavargaja Bala.** Art. 30: Moolatrikona 45 **in the Rasi only**; own 30, great friend 22.5, friend 15, neutral 7.5, enemy 3.75, bitter enemy 1.875 in D1, D2, D3, D7, D9, D12, D30; compound relationship from the Rasi chart. `PROJECT_DERIVED`: the Moolatrikona degree ranges are the locked Phase 6 table (Raman refers to his *Manual*, not read). 47 of the 49 printed relation cells match the locked Phase 5 varga schemes; the other two are recorded (SR-24).
- **SR-06 Drekkana Bala.** `UNRESOLVED_CONFLICT`, handled by an explicit request field with **no default**: `drekkana_reading = text_article_36` (male first, hermaphrodite middle, female last, as Art. 36 says) or `worked_example_12` (male first, female second, neuter third, as his own Example 12 applies, which is also the BPHS translation's order).
- **SR-07 Ojayugma and Kendra Bala.** As SB-05; Kendra by signs from the Lagna, which Raman adopts as "the view of Parasara" (Art. 33).
- **SR-08 Dig Bala.** As SB-06; his powerless points are the Nadir, descendant, meridian and ascendant (Art. 44-45).
- **SR-09 Paksha Bala.** Art. 52-55: as SB-08, but the Moon's value is always doubled, and the Moon's own benefic/malefic status has **no default** (`moon_paksha_reading`): Art. 53 says the increasing Moon is benefic and "i.e." benefic from the 8th day of the bright half to the 8th day of the dark half -- two different rules (`waxing_half`: elongation below 180 degrees; `eighth_day_window`: 84 to 276 degrees, both 8th days included). Mercury's "affliction" follows the locked Phase 6 same-sign convention; a mixed association is `NOT_EVALUABLE(mercury_mixed_association)`.
- **SR-10 Nathonnatha Bala.** As SB-07 (Art. 47-51); his worked values (48.84, 11.16, 60) are reproduced from his Unnata of 24.42 ghatis.
- **SR-11 Tribhaga Bala.** As SB-09 (Art. 56-57).
- **SR-12 Abda and Masa Bala.** Art. 58-66: 15 to the lord of the 360-day year and 30 to the lord of the 30-day month, from the **condensed ahargana** = days from 1 January 1900 + 26,543 (Art. 64), remainders counted from Wednesday. The ahargana day is the Hindu (sunrise) day. `ENGINEERING_EVIDENCE`: it reproduces Raman's Standard Horoscope (33,405: Saturn, Mercury) and, independently, the example in Santhanam's BPHS Ch. 27 note (1 June 1984: Jupiter, Venus, a Friday), whose ahargana epoch is different; the weekday it implies agrees with the calendar for every tested date.
- **SR-13 Vara Bala.** 45 to the weekday lord, the day from sunrise.
- **SR-14 Hora Bala.** Art. 68-70: 60 to the lord of the equal one-hour Hora counted from sunrise, the first Hora being the weekday lord, then the order Sun, Venus, Mercury, Moon, Saturn, Jupiter, Mars.
- **SR-15 Ayana Bala.** Art. 71-75: declination from Raman's 15-degree table (362, 341, 299, 236, 150, 52 arcminutes; maximum 24 degrees) applied to the tropical longitude of date (Lahiri longitude + Lahiri ayanamsa; Raman's "Sayana" longitude); (24 +/- kranti)/48 x 60, north additive for the Sun, Mars, Jupiter, Venus, south additive for the Moon and Saturn, always additive for Mercury; the Sun's value doubled.
- **SR-16 Yuddha Bala.** Art. 76-77: planets from Mars to Saturn less than one degree apart are at war; the one with the lesser longitude wins; the value is |difference of (Sthana + Dig + Kala up to Hora)| / |difference of disc diameters| (Mars 9.4, Mercury 6.6, Jupiter 190.4, Venus 16.6, Saturn 158.0 arc seconds), added to the winner and subtracted from the loser. `PROJECT_DERIVED` `NOT_EVALUABLE(planetary_war_unresolved)`: a planet in two wars, identical longitudes, or a war across 0 Aries (where "lesser longitude" is ambiguous). The Sun and Moon: `not_applicable`.
- **SR-17 Cheshta Bala.** Art. 79-107 and Tables IV-IX: sighrocca - (mean longitude + true longitude)/2, reduced to at most 180 degrees, / 3. Mean elements from Raman's tables (epoch 1 January 1900, mean midnight at 76 E; degrees per day from the ten-thousand-day columns: Sun 0.98560265, Mars 0.524019, Jupiter 0.083096, Saturn 0.033439, Mercury's sighrocca 4.092318, Venus's sighrocca 1.602146; epoch values 257.4568, 270.22, 220.04, 236.74, 164, 328.51 degrees; Raman's linear corrections in years since 1900). The superior planets' sighrocca and the inferior planets' mean longitude are the mean Sun. `PROJECT_DERIVED`: because the tables are in Raman's own sidereal frame, the true longitude is taken in that frame (tropical minus the Swiss Ephemeris Raman ayanamsa, read with `ephemeris.reference_ayanamsa_with_nutation_degrees` and never applied to the chart); the kendra is frame-consistent. Years outside 1800-2100 are `NOT_EVALUABLE(cheshta_tables_out_of_range)`, an engineering bound on extrapolating 1900-based tables.
- **SR-18 Sun and Moon Cheshta.** Raman gives Cheshta Bala for Mars to Saturn only (Art. 79) and his totals omit it for the luminaries: `not_applicable` (counted as zero), unlike SB-15.
- **SR-19 Naisargika Bala.** As SB-16 (Art. 108).
- **SR-20 Drik Bala.** Art. 109-120: one quarter of the Dristi Pinda, the signed sum of the aspects of the other six planets (Sripati values: (K-30)/2, (K-60)+15, (120-K)/2+30, 150-K, (K-150)x2, (300-K)/2 for K from 30 to 300 degrees; plus the special aspects of Saturn +45 at 60-90 and 270-300, Jupiter +30 at 120-150 and 240-270, Mars +15 at 90-120 and 210-240); benefics add, malefics subtract (waxing Moon benefic, waning malefic, locked Phase 6 conventions). The nodes neither cast nor receive aspects. A needed nature that cannot be determined makes the component `NOT_EVALUABLE(drik_nature_undetermined)`.
- **SR-21 Totals.** Sthana, Kala and the Shadbala Pinda are sums of their parts; `not_applicable` parts count as zero; Drik is signed. Totals are reported only when no part is `NOT_EVALUABLE`, with rupas (virupas / 60). No "strong/weak" label is produced (Raman's Art. 122 thresholds are interpretive and are not implemented).
- **SR-22 NOT_EVALUABLE.** Unknown birth time (everything except Naisargika), no sunrise/sunset near the instant (Tribhaga, Abda, Masa, Vara, Hora), Mercury's mixed association (Paksha), SR-16, SR-17 and SR-20 conditions.
- **SR-23 Versions.** Raman-profile results record `standards_version = 1.21.0`; astro-engine 0.10.0.
- **SR-24 Recorded book slips (the rule is followed).** Ayana Mars printed 1.40 (rule 1.90) and Venus 23.80 (rule 24.30); the Moon's Hora cell printed 30.000 where the relation and total use 7.5; Mars D30 "own" and Saturn D7 "neutral" differ from the locked varga schemes; the p. 75 angle 225 deg 02 min for Mars on Saturn should be 255 deg 02 min; four Drishti cells are off by 0.2-0.6; Saturn's Kala 115.740 versus 115.690; Venus's sighrocca listed once as 151.35 (158.35 used); Mercury's sighrocca epoch 160 in the table header (164 in the text and example); the Mars total 322.590 (its column sums to 322.890); Uchcha values printed to one decimal, sometimes truncated. Raman's planetary positions themselves differ from Swiss Ephemeris by up to 1.8 degrees, so end-to-end totals are compared component by component, never as totals.

## Chinese standards (Phase 9 WP-H methodology lock, v1.18.0)

Defines the standard and contract for **Phase 9 WP-H**, `Phases.md` "Chinese astrology". `Phases.md` does not say which Chinese system; this lock chooses the **Four Pillars (BaZi) calendar pillars** as the foundation because they are a deterministic calendar calculation with official astronomical references, and records the choice as `PROJECT_DERIVED`. Zi Wei Dou Shu, the popular lunar-new-year zodiac, luck cycles and every interpretive layer are not implemented. Sources: `research/ASTROLOGY_SOURCES.md` Group 19.

- **CN-01 Scope.** Profile `CHINESE_BAZI_FOUR_PILLARS_SOLAR_TERMS_V1`, module `pandit_astro_engine/chinese/`, system tag `chinese_bazi`; it imports no Vedic, Western or KP module and no `swisseph`.
- **CN-02 Identifiers.** Stems and branches by Pinyin identifier (the characters are presentation data). `SOURCE_SUPPORTED` (《三命通會》 Sanming Tonghui, Wikisource transcription, `WEB-TRANSCRIPTION-SOURCE-LANGUAGE (unreviewed)`): stem elements and polarity (卷二 論十干合), branch elements (卷三 論十干祿; 辰戌丑未 earth), branch animals and polarity (卷二 論地支屬相).
- **CN-03 Solar terms.** `SOURCE_SUPPORTED` (Hong Kong Observatory, `DOCUMENTATION-DIRECT`): the instants the Sun's apparent tropical longitude reaches multiples of 15°. Computed with the Phase 4 adapter (no ayanamsa) by Newton iteration to 1e-9 day.
- **CN-04 Year pillar.** `MODERN_TRADITION`: changes at 立春 (315°); index (Y − 4) mod 60. The text counts months from 寅, opened by the jie terms, but was not found stating the year boundary directly.
- **CN-05 Month pillar.** `SOURCE_SUPPORTED`: months open at the twelve jie terms; the 寅 month's stem by 卷二 論遁月時 "甲己之年丙作首 …".
- **CN-06 Day pillar.** `ENGINEERING_EVIDENCE`: the continuous sixty-day count, index (JDN + 49) mod 60 (甲子 = 0), checked against the almanac reading 1 January 2000 = 己卯年 丙子月 戊午日 (several secondary almanac sites agree; confidence MEDIUM; Academia Sinica's converter could not be queried automatically).
- **CN-07 Hour pillar.** `SOURCE_SUPPORTED`: 子 = 23:00-01:00; the 子 hour's stem by 論遁月時 "甲己還加甲 …".
- **CN-08 Time basis.** `UNRESOLVED_CONFLICT`, no default: `clock_time`, `local_mean_solar_time` (UT + longitude/15) or `local_apparent_solar_time` (plus the equation of time).
- **CN-09 Day boundary.** `UNRESOLVED_CONFLICT`, no default: `zi_hour_2300` (day and hour change at 23:00) or `midnight`.
- **CN-10 Late 子 hour.** With the midnight boundary, the 23:00-24:00 hour stem is `NOT_EVALUABLE(late_zi_hour_stem_unresolved)`: schools differ on whether it takes the current or the next day's stem.
- **CN-11 Unknown time.** The hour is `NOT_EVALUABLE(birth_time_unknown)`; the day is evaluated only for clock time with the midnight boundary; year and month are `NOT_EVALUABLE(solar_term_on_birth_date)` when a jie falls on the birth date.
- **CN-12 Not evaluated.** Luck cycles (大運) need the person's sex, which Pandit Ji does not collect, so they are not produced; hidden stems, ten gods and Na Yin are not implemented.
- **CN-13 Versions.** Chinese results record `standards_version = 1.18.0`.
- **CN-14 Evidence.** `ENGINEERING_EVIDENCE`: all 216 HKO solar-term instants for 2020-2028 are reproduced within 30.2 s (HKO rounds to the minute), and HKO's year names for 1901, 1950, 1984, 2000, 2024, 2044 and 2100 match (fixture `hko_solar_terms_and_year_names.json`). The classical text was read in a web transcription by a non-qualified reader, not in a critical edition.

## Tarot standards (Phase 9 WP-I methodology lock, v1.19.0)

Defines the standard and contract for **Phase 9 WP-I**, `Phases.md` "Tarot": a deterministic card-layout foundation; interpretation is deferred to the knowledge and agent phases. Sources: Group 20 (A. E. Waite, *The Pictorial Key to the Tarot*, 1910/1911, English Wikisource transcription, `WEB-TRANSCRIPTION-ORIGINAL-ENGLISH`).

- **TA-01 Scope and placement.** `PROJECT_DERIVED`: `pandit_astro_engine/tarot/` (astro-engine is the deterministic-computation service; Tarot uses no ephemeris). System tag `tarot`. No card meanings are stored; results carry `interpretation = None` and a notice that a layout is a traditional practice, not a verified statement about the future and not medical, legal, financial or safety advice.
- **TA-02 Deck.** `TAROT_DECK_WAITE_SMITH_1910`, `SOURCE_SUPPORTED`: 22 Trumps Major, 0 (The Fool) to XXI, in Waite's numbering (Strength VIII and Justice XI, "interchanged" by Waite), and 56 Lesser Arcana in Wands, Cups, Swords and Pentacles, King to Ace. Stable IDs (`major_08_strength`, `cups_ace`, ...).
- **TA-03 Spreads.** `TAROT_SPREAD_WAITE_CELTIC_METHOD_1910` (`SOURCE_SUPPORTED`, Part III: ten positions from "This covers him" to "What will come", around a Significator); `TAROT_SPREAD_SINGLE_CARD` and `TAROT_SPREAD_THREE_CARD_UNLABELLED` (`engineering_convention`; the popular past/present/future labels are not sourced and not used).
- **TA-04 Seeds.** The caller supplies a non-empty seed; the engine never uses system randomness or the clock (tested by an import scan). Results record the SHA-256 of the seed, never the seed.
- **TA-05 Stream and shuffle.** `SHA256_COUNTER_REJECTION_V1`: SHA-256 of a seed-derived key and a counter, 32-bit words, rejection sampling; Fisher-Yates shuffle; the layout is dealt from the top. Identical on every platform and Python version.
- **TA-06 Reversals.** `allow_reversals` is required (no default); when allowed, each placed card's orientation is one further draw from the stream.
- **TA-07 User selection.** A user-chosen layout is preserved exactly (cards and orientations, in position order); unknown or repeated cards and wrong counts are rejected.
- **TA-08 Significator.** Required for the Celtic method and chosen explicitly by the caller; never inferred (Waite chooses it by sex and age, which Pandit Ji does not collect). It is removed from the pack before shuffling.
- **TA-09 Physical procedure.** `PROJECT_DERIVED`: Waite's "shuffle and cut the rest of the pack three times" is modelled by the seeded shuffle, an engineering equivalent, not a claim that the two are the same act.
- **TA-10 Versions and rights.** Layouts record `standards_version = 1.19.0`. Waite (d. 1942) and Pamela Colman Smith (d. 1951) are public domain in India and the US; only names and numbers are held, no images and no guidebook text.
- **TA-11 Provenance (v1.21.0).** Every layout carries provenance entries for the deck, the spread and (for a seeded draw) the stream, each with its evidence label and source; the field is additive and the card placements are unchanged.

## Phase 9 closure: research-only, deferred and excluded systems (v1.20.0)

Records the systems `Phases.md` Phase 9 lists that are **not implemented** after this phase's research, and why (sources: `research/ASTROLOGY_SOURCES.md` Group 21). None may be implemented without a new methodology lock, and none is presented as available.

- **P9-01 Lal Kitab — research only, deferred.** Five Urdu editions (1939, 1940, 1941, 1942, 1952) attributed to Pt. Roop Chand Joshi (authorship `CONTESTED`); Joshi died in 1982, so the texts are presumed in copyright in India until the end of 2042. No edition was read: Pandit Ji has no qualified Urdu reader, and the popular "fixed house chart" and remedy lists circulate only in secondary retellings. Its remedies raise the safety concerns of §Remedies. The blocker is source access and language, not engineering.
- **P9-02 Nadi — research only, not computable.** Traditional Nadi practice matches a person to pre-written palm-leaf texts held by private readers; the texts are not public, not verifiable and not deterministic. Printed "Nadi" works (for example the Chandra Kala Nadi) are separate interpretive texts, not a calculation method. Pandit Ji will not present generic chart calculations as Nadi readings.
- **P9-03 General Horary — deferred, tradition not chosen.** Western horary (Lilly, *Christian Astrology*, already registered) and Vedic Prashna (Prasna Marga) are distinct traditions, and `Phases.md` does not say which is meant. A chart for the question moment can already be built with §Western standards or §KP standards; judgment rules, planetary hours and radicality are not locked. KP Horary is implemented (KP-11).
- **P9-04 Vastu — deferred.** Candidate sources (Mayamata, Manasara, Brihat Samhita Ch. 53) were identified, not read. The product collects no building data, and `PRODUCT_POLICIES.md` data minimisation forbids collecting it for a feature not yet built; §Astrology systems already places cross-domain Vastu in Phase 17. Structural, electrical or construction advice is out of scope in any case.
- **P9-05 Feng Shui — deferred.** Flying Stars and Eight Mansions were identified as the computational schools; Eight Mansions needs the person's sex (not collected, so it would be `NOT_EVALUABLE`) and both need building orientation and construction period. No source was read.
- **P9-06 Ayurdaya — excluded pending a product-policy decision.** BPHS Ch. 43 (Pindayu, Nisargayu, Amsayu; read in Group 5) yields a lifespan in years. `Phases.md` requires a methodology and product-policy audit first and allows "structural facts only, not lifespan predictions"; every Ayurdaya output is itself a lifespan figure, and `PRODUCT_POLICIES.md` rules out death and fear-based claims. The audit recommends exclusion unless the owner defines a non-harmful output contract. It also depends on Shadbala totals, which are not evaluable (SB-17).
- **P9-07 Jaimini Dashas — research only.** Chara Dasha and the other sign dashas have several incompatible modern reconstructions; BPHS's dasha chapters and the Jaimini Sutras were not re-read this phase. Not implemented.
- **P9-08 Evidence bundle.** None of WP-E to WP-I is integrated into the rule-engine `EvidenceBundle`; each needs its own gate (as for WP-D).
- **P9-10 Owner decisions of 2026-09-25.** The owner accepted the deferrals P9-01 to P9-05 and P9-07 and the absence of Tarot meanings; kept Ayurdaya excluded (P9-06) because `PRODUCT_POLICIES.md` prohibits lifespan outputs; reopened Shadbala, now answered by the separate Raman profile (SR-01 to SR-24); and asked for evidence-bundle integration, now EV-01 to EV-10.
- **P9-11 Phase 9 status.** With those decisions, the Phase 9 roadmap is complete **as scoped by the owner**: every listed system is either implemented (with the limits recorded in its section) or explicitly deferred or excluded by owner decision. The deferred systems are not implemented and must not be presented as available.
- **P9-09 Roadmap note.** Arudha Pada and Karakamsa (WP-G) are not named in `Phases.md` Phase 9, which lists only Chara Karakas and Rashi Drishti under Jaimini; they were implemented on the project owner's Phase 9 directive, and the gap is reported, not edited into `Phases.md`.

## Phase 9 evidence-bundle integration (v1.21.0, EV-01 to EV-10)

- **EV-01 Scope.** The rule-engine `EvidenceBundle` gains five additive, optional sections -- `kp`, `shadbala`, `jaimini`, `chinese`, `tarot` -- carried by `pandit_rule_engine.phase9_evidence`. Transport only: the adapters validate and re-shape astro-engine JSON and compute nothing; the rule engine still does not import astro-engine.
- **EV-02 Backward compatibility.** Each section is omitted from the serialized bundle when absent, so a bundle built without them serializes and hashes exactly as before (tested with explicit `None` against omitted arguments).
- **EV-03 KP.** One KP natal or horary result per bundle (`kind`), with profile IDs, cusps, lordships, significators, Ruling Planets, the horary entry, provenance and warnings; status/reason invariants enforced (a successful cusp result must carry 12 cusps).
- **EV-04 Shadbala.** Exactly one profile per bundle -- the BPHS verse profile or the Raman profile -- never both in one section and never merged; the Raman readings are kept. A total marked successful while any leaf component is `NOT_EVALUABLE` is rejected, so a total can never hide a missing component.
- **EV-05 Jaimini.** The `JaiminiFacts` object (JN-19): twelve Bhava Padas required; Karakamsa success requires a sign.
- **EV-06 Chinese.** The four pillars with status/reason invariants, the time basis, day boundary and the `not_evaluated` list (luck cycles need the person's sex, not collected).
- **EV-07 Tarot.** A layout with no interpretation (rejected otherwise), no repeated card, the seed only as its hash, and its provenance. It carries no chart fact and no rule reads it.
- **EV-08 Hashing.** Each section records `facts_hash`, the SHA-256 of the canonical JSON of the whole astro-engine input, so the source result is reproducible; adding a section changes the bundle hash deterministically.
- **EV-09 Rules.** No shipped rule reads any of these sections; they are evidence for later phases.
- **EV-10 Versions.** rule-engine 0.10.0; each section keeps the `standards_version` its astro-engine result recorded.

## Nakshatra standards

Defines the standard and contract that **Phase 5** (as part of the Kundli engine) implements — not implemented in Phase 1.

- **27 Nakshatras** spanning the full 360° sidereal zodiac.
- **Zodiacal span**: 13°20′ (360° ÷ 27) per Nakshatra.
- **Padas**: four padas per Nakshatra, each 3°20′ (13°20′ ÷ 4).
- **Planetary lord sequence**: the same nine Vimshottari lords (Ketu, Venus, Sun, Moon, Mars, Rahu, Jupiter, Saturn, Mercury), repeating three times across the 27 Nakshatras (9 × 3 = 27).
- **Boundary handling**: Nakshatra and pada boundaries are computed from the Moon's (or other relevant planet's) exact sidereal longitude after ayanamsa correction, at full internal precision (degrees/minutes/seconds). A position falling essentially exactly on a boundary must be flagged for review rather than silently rounded to one side.
- **Boundary classification convention (v1.4.1; a Pandit Ji engineering convention, not a rule stated by any classical source)**: the classical texts read so far list shared endpoints (for example 13°20′ closes Ashwini and opens Bharani) and state no inclusivity rule, so this standard fixes one. Classification uses half-open intervals, lower bound inclusive and upper bound exclusive, for both Nakshatras and Padas: a longitude exactly on a boundary belongs to the upper Nakshatra or Pada, and 360° is 0° (Ashwini, Pada 1).
  - **Normalization**: the input longitude is first normalized with the existing floating-point modulo 360. No new normalization rule is introduced.
  - **Exact classification**: the normalized float is then classified by exact rational arithmetic on its exact value: Nakshatra index = ⌊n × 27 ÷ 360⌋ and Pada slot = ⌊n × 108 ÷ 360⌋ (Pada = slot mod 4 + 1), taken modulo 360 so that a normalized 360.0 maps to 0. There is no intermediate rounding and no tolerance. A float divisor such as 360.0/27.0 must not be used, because it is not exactly 13°20′ and can place an exactly representable boundary in the lower bucket (for example 40.0° must be Rohini, Pada 1, and 10.0° must be Ashwini, Pada 4).
  - **Display rounding** never decides classification.
  - **`near_boundary` flag**: unchanged. It remains an informational numeric flag (an epsilon of 1e-6°) raised for review near a Nakshatra or Pada boundary; it does not affect which Nakshatra or Pada is assigned.
  - **Known unchanged behaviour (not approved for change)**: because normalization uses the floating-point modulo, a negative longitude smaller in magnitude than about 2.8e-14° normalizes to 360.0 and therefore classifies as 0° (Ashwini), not Revati. Ephemeris longitudes are already in [0, 360), so this input does not occur in the pipeline.
  - **Scope**: this convention only fixes the side of a boundary. It does not select the Vimshottari balance-at-birth method, the year length, leap-year handling or rounding, which remain open Phase 7 decisions (§Vimshottari Dasha).
- **Moon's sidereal longitude → Janma Nakshatra**: the birth (Janma) Nakshatra and pada are derived by dividing the Moon's sidereal longitude at birth by 13°20′ (Nakshatra) and further by 3°20′ (pada), with the remainder giving both the pada and the dasha-balance fraction used in §Vimshottari Dasha.
- **Precision/reproducibility**: same precision and reproducibility requirements as the rest of the Default Vedic profile.

## Yoga / Dosha standards

Defines the standard for representing and evaluating Yogas and Doshas — the schema the rule engine implements in **Phase 6 (Vedic Astrology Rule Engine)**, not the rule content itself and not implemented in Phase 1.

Every Yoga/Dosha rule must be represented with:
- Rule-based detection logic (a pattern over chart facts, evaluated deterministically — never left to AI judgment)
- Required chart conditions (the facts that must hold for the rule to trigger)
- Supporting conditions (factors that strengthen the interpretation without being individually required)
- Cancellation/mitigation conditions where the tradition defines them (a rule may be triggered but reported as fully or partially cancelled per classical exception rules)
- Strength/severity/context (how strongly the rule applies given the specific chart, not a flat yes/no)
- Source/tradition (which text/school the rule comes from — see `research/ASTROLOGY_SOURCES.md`)
- Exceptions (documented exception conditions from the source tradition)
- Interpretation (the traditional meaning associated with the triggered rule — narrative phrasing is the AI's job at response time, not stored as free text in the rule itself)
- Timing relevance (which dasha/transit periods, if any, the rule's effects are most associated with)
- Conflicting rules (an explicit list of other rule IDs this rule is known to contradict, so the evidence-bundle contradiction analysis in `docs/ARCHITECTURE.md` §7 can surface both sides)

**Do NOT invent a universal list of Yogas/Doshas where traditions disagree.** Where Parashari, Jaimini, Lal Kitab, KP, or other schools define a given Yoga/Dosha differently, each school's version is recorded as its own tagged rule under its own source/tradition, and the evidence bundle presents both rather than the system silently picking a winner.

## Phase 6 rule-engine methodology

Defines the standard and contract that **Phase 6 (Vedic Astrology Rule Engine)** implements. Added in v1.4.0 from the Phase 6 methodology research (`research/ASTROLOGY_SOURCES.md`, Groups 1–8), per explicit project-owner approval. Rules evaluate facts from Phases 4–5 (and later phases where declared); the rule engine never calculates astronomy, dashas, transits or Panchang, and the AI layer never calculates astrology.

### Source tiers

- **Tier 1**: primary classical Sanskrit text or critical edition, reviewed by a qualified reader. No source has yet been reviewed at this level; no rule may be labelled `SANSKRIT-LEVEL VERIFIED` until one is.
- **Tier 2**: recognised scholarly or classical translation (for example the Santhanam and Kapoor translations of Brihat Parashara Hora Shastra, Sastri's Phaladeepika). Can establish a canonical rule, labelled `TRANSLATION-LEVEL VERIFIED`.
- **Tier 3**: traditional secondary source or translator's commentary. Supplies variants and supporting evidence; a translator's note is never treated as a verse and cannot by itself establish a canonical rule.
- **Tier 4**: modern practitioner source. May justify a separately tagged `MODERN_TRADITION` profile, never a canonical or classical rule.
- **Tier 5**: general internet content. Never evidence for a rule.

### Source profiles and rule identifiers

- Rules are **source-specific profiles**. Where traditions differ materially, each is a separate rule with its own profile ID; there is no generic `GAJAKESARI`, `KEMADRUMA` or `MANGAL_DOSHA` rule.
- Profile IDs are upper-case and underscore-separated: work code, translator or edition code where it matters, family, then chapter and verse (for example `BPHS_SAN_GAJAKESARI_36_3_4`, `BPHS_KAPOOR_80_47_49_MARS_HOUSES`, `JP_KUJA_DOSHA`, `MODERN_KAAL_SARP_<SOURCE>`). The registry of finalized profile IDs is in `research/ASTROLOGY_SOURCES.md` §6.
- Every profile records: source, edition, translator, source location, source version, astrology system, school, profile ID, conditions, readings, exceptions, cancellations, dependencies, interpretation tags, priority, and ambiguity/conflict metadata.
- **Priority is metadata only.** It may order display or evaluation where explicitly justified. It never removes conflicting evidence and never means "objectively true".
- Interpretation is a structured tag (domain, signification, effect class), never prose. Traditional wording belongs to the knowledge base (`Phases.md` Phase 12).

### Ambiguity-preserving evaluation

- A rule may carry several **readings**, each with a reading ID and its source. Each reading is evaluated separately.
- If all readings give the same result, that result is returned with every reading ID and source ID retained. If they differ, the result is `NOT_EVALUABLE(reading_ambiguous)` with every reading's outcome retained. A reading is never silently chosen.
- Source conflicts between profiles are provenance and profile metadata, grouped by a conflict group; they are not runtime truth statuses unless they make one rule's own reading ambiguous.

### Result statuses and reason codes

- Statuses: `TRIGGERED`, `NOT_TRIGGERED`, `CANCELLED`, `PARTIALLY_CANCELLED`, `NOT_EVALUABLE(reason)`. `PARTIALLY_CANCELLED` is used only where the source states a partial cancellation.
- Reason codes: `reading_ambiguous`, `missing_dependency:<id>`, `requires_shadbala`, `requires_dasha`, `requires_partial_drishti`, `requires_gender`, `requires_partner_chart`, `requires_moolatrikona`, `node_participation_unspecified`, `varga_scheme_conflict`, `time_base_unspecified`, `source_profile_not_selected`, `scope`, `not_specified_by_source`, `condition_absent_in_source`.
- A source silent on a point gives `not_specified_by_source`. A dependency owned by a later phase gives `missing_dependency:<id>`. A rule is never evaluated to `NOT_TRIGGERED` because an input was unavailable.

### Aspect refinement

- Phase 6 uses only the Phase 5 full-sign graha drishti (§Planetary aspects standard). It never substitutes full-sign aspect for a rule that needs partial, degree-based or Jaimini sign aspect.
- For a condition phrased as "aspected by X": if a full-sign aspect is present the condition is definitely true. For "unaspected": if no full-sign aspect is present, a partial aspect (BPHS Ch. 26 v. 2–5) may still exist, so the result is `NOT_EVALUABLE(requires_partial_drishti)` until Phase 9 provides partial aspects. BPHS Ch. 26 v. 2–5 gives partial aspects only on the 3rd/10th, 5th/9th and 4th/8th from the aspecting planet (the 7th is always full), so from any other house no aspect exists and "unaspected" is definitely true there; `requires_partial_drishti` applies only from those six houses (implementation clarification recorded during Phase 6 implementation).
- Jaimini rashi drishti and degree-based drishti are separate systems and are never blended with graha drishti.

### Strength

- "Strong", "stronger" and "weak" have no technical definition in the reviewed BPHS chapters. Rules that use them return `NOT_EVALUABLE(requires_shadbala)` until Shadbala (`Phases.md` Phase 9) exists. Rules that state an explicit dignity or house condition are evaluated normally. No composite strength score is defined.

### Natural relationships, natural nature and functional nature

- **Natural relationships** (Brihat Parashara Hora Shastra Ch. 3 v. 55, Santhanam translation, table checked against the page image). Friend / enemy / equal:

| Planet | Friends | Enemies | Equals |
|---|---|---|---|
| Sun | Moon, Mars, Jupiter | Venus, Saturn | Mercury |
| Moon | Sun, Mercury | none | Mars, Jupiter, Venus, Saturn |
| Mars | Sun, Moon, Jupiter | Mercury | Venus, Saturn |
| Mercury | Sun, Venus | Moon | Mars, Jupiter, Saturn |
| Jupiter | Sun, Moon, Mars | Mercury, Venus | Saturn |
| Venus | Mercury, Saturn | Sun, Moon | Mars, Jupiter |
| Saturn | Mercury, Venus | Sun, Moon, Mars | Jupiter |

  The Moon row follows the statement the translator quotes from the Benares (Chaukhamba) edition that the Moon has no enemy; the v. 55 counting formula alone would give a different Moon row. This is recorded as a bounded source conflict in `research/ASTROLOGY_SOURCES.md`.
- **Temporal relationship** (v. 56): a planet in the 2nd, 3rd, 4th, 10th, 11th or 12th sign from another is its temporal friend; otherwise its temporal enemy.
- **Compound relationship** (v. 57–58): natural plus temporal: friend + friend gives great friend; friend + enemy gives neutral; friend + equal gives friend; enemy + equal gives enemy; enemy + enemy gives great enemy; equal + equal gives neutral. The output field is `relationship_kind`.
- **Rahu and Ketu are excluded** from the relationship tables; the only node relationships found are in an unsourced translator's note (Tier 3), which is not used.
- **Natural benefic/malefic** (v. 11): Sun, Saturn, Mars, the waning Moon, Rahu and Ketu are malefic; the other planets are benefic; Mercury is malefic when joined with a malefic. Two Pandit Ji conventions, labelled as such and not classical: the Moon is waxing when its elongation from the Sun is 0°–180° and waning otherwise, with a near-boundary flag; Mercury's "joined with" means same-sign conjunction only. Mercury's values are benefic, malefic, `mixed` or `none`.
- **Functional nature**: the canonical BPHS functional-nature source is the 84-cell per-Lagna table of Ch. 34 v. 19–44 (12 Lagnas × 7 planets, checked cell by cell against the page images). Source labels are stored exactly as the source gives them (for example auspicious, malefic, killer, yogakaraka, neutral, association-dependent, and their combinations) and are never converted into a good/bad score. Cells the verse does not address are `not_specified_by_source`. No replacement universal algorithm is derived. The general rules of Ch. 34 v. 2–17 remain separate structural rules, not a functional-nature algorithm.

### Node policy (Rahu and Ketu)

- Natural nature: as in Ch. 3 v. 11 (malefic). Functional nature: no node cell exists in the Ch. 34 table and none is invented; Ch. 34 v. 16–17 (nodes act by association and house; a node in an angle with a trinal lord, or in a trine with an angular lord, is a yogakaraka) is a separate rule.
- No universal Parashari lordship, relationship table, dignity system, sign-lord inheritance, dispositor inheritance or conjunction inheritance is defined for nodes. Combustion follows §Combustion standard.
- A node participates in a rule only where the source supports it. If node participation would change the result and the source is silent: `NOT_EVALUABLE(node_participation_unspecified)`. Source-specific node traditions may be added later as separate profiles.

### Moolatrikona (Phase 6 fact)

- Moolatrikona is a Phase 6 data-driven fact, separate from the Phase 5 dignity model, which is unchanged. It is derived from sign and degree (Brihat Parashara Hora Shastra Ch. 3 v. 51–54): Sun Leo 0°–20°; Moon Taurus 3°–30°; Mars Aries 0°–12°; Mercury Virgo 15°–20°; Jupiter Sagittarius 0°–10°; Venus Libra 0°–15°; Saturn Aquarius 0°–20°. A rule that needs it while the fact is unavailable returns `NOT_EVALUABLE(requires_moolatrikona)`.

### Vargas and D27

- **D27 (Nakshatramsa / Bhamsa) is `UNRESOLVED`.** The §Divisional charts formula (Aries, Cancer, Libra by modality) is unchanged. BPHS Ch. 6 v. 24–26 says the count begins "from Aries and the movable signs"; the verse names no element or modality, so both the modality rule and the translator's element table are interpretations. The grammar needs qualified Sanskrit review. Any Phase 6 rule that uses D27 returns `NOT_EVALUABLE(varga_scheme_conflict)`.

### Dependencies owned by other phases

- Dasha (Phase 7), transits (Phase 8), Shadbala, partial and degree drishti, Jaimini and longevity methods (Phase 9), and Panchang, sunrise-based special points and Tara Balam (Phase 10) are not calculated by Phase 6. A rule that needs one returns `NOT_EVALUABLE(missing_dependency:<id>)` or the specific reason code above.

### Evidence bundle and reproducibility

- The evidence bundle preserves: the chart and calculation snapshot; the calculation configuration (ayanamsa, node model, house system, timezone, coordinates); the calculation-engine, standards, rule-engine and ruleset versions; the ruleset content hash; rule IDs, source profiles and reading IDs; triggered, non-triggered and `NOT_EVALUABLE` results; conflicts, unresolved dependencies and provenance. The same input, configuration, ruleset hash and engine version must produce the same bundle.
- **Standards version per phase**: each result records the standards version under which it was produced and is never re-labelled when the document advances. A Phase 5 calculation snapshot keeps `standards_version = 1.3.0` (its formulas are unchanged in v1.4.0); Phase 6 rule evaluation records `standards_version = 1.4.0`. The evidence bundle therefore carries both the calculation snapshot's standards version and the rule evaluation's.

## Panchang / Muhurta standards

Defines the standard and contract that **Phase 10 (Panchang, Muhurta & Calendar Engine)** implements — not implemented in Phase 1.

### Panchang
- **Tithi**: the angular distance between Moon and Sun's sidereal longitudes, divided by 12° (30 tithis per synodic lunar month).
- **Vara**: the weekday, reckoned sunrise-to-sunrise per traditional Vedic civil-day convention, not midnight-to-midnight.
- **Nakshatra**: as defined in §Nakshatra standards, computed for the Panchang date rather than a birth date.
- **Yoga** (Panchang Yoga, distinct from natal Yogas above): the sum of the Sun's and Moon's sidereal longitudes, divided by 13°20′ (27 Yogas).
- **Karana**: half of a Tithi; 11 Karanas cycling across a lunar month (7 repeating + 4 fixed, per classical convention).
- **Sunrise/sunset dependency**: Panchang element transitions (especially Tithi and the civil day itself) are reckoned from location-specific sunrise, not a fixed clock time — the engine must compute actual sunrise/sunset for the query location and date, not assume a constant.
- **Local location/timezone handling**: Panchang is inherently location-dependent (two locations can be in different Tithis at the same clock instant); the query location's coordinates and timezone must always be explicit inputs.
- **Regional/calendar configuration**: some regional calendar conventions (e.g., Tamil, Bengali) differ from the standard pan-Indian convention; the regional convention in use must be an explicit, recorded configuration value, defaulting to the standard (Drik-Ganita-based) pan-Indian convention.
- **Reproducibility**: identical date, location, and configuration must reproduce identical Panchang values.

### Muhurta
- **Purpose-specific timing**: different purposes (marriage/Vivah, Griha Pravesh, Mundan, and other Shubh Muhurat categories) have distinct classical rule sets; every Muhurta result must be tagged with the purpose it was computed for.
- **Location dependence**: inherits Panchang's location dependency in full.
- **Panchang dependencies**: Muhurta selection is built on top of the Panchang elements above, plus additional derived factors (Rahu Kaal, Choghadiya, Panchak, Bhadra, Tara Balam, Chandra Balam).
- **Configurable traditional rules**: different traditions/regions apply different Muhurta rule sets; the rule set in use must be versioned and tagged by source, following the same rule-representation schema as §Yoga/Dosha standards.
- **Auspicious/inauspicious criteria**: represented using the same evidence/rule structure as Yogas/Doshas (required conditions, supporting/conflicting factors, source/tradition), not as an unstructured label.
- **Source/tradition identification**: every Muhurta rule cites its source text.
- **Boundary handling**: Muhurta windows have precise start/end timestamps derived from the underlying Panchang element transitions — boundaries are exact moments, not whole days.

## Numerology standards

Defines the standard and contract that **Phase 11 (Numerology + Compatibility)** implements — not implemented in Phase 1.

- **Supported system(s)**: the Vedic/Chaldean-influenced Indian numerology framework (Moolank + Bhagyank) is the default; Western Pythagorean numerology is supported as an explicit, separately-configured alternate system. **The two systems must never be silently combined** — a result must be tagged with exactly one system.
- **Name-number methodology**: letter-to-number mapping depends on the configured system — Chaldean-style mapping uses sound-based values 1-8 (no letter maps to 9); Pythagorean mapping uses sequential values 1-9 by letter order. The mapping table in use must be an explicit, recorded configuration value.
- **Date-of-birth methodology / Moolank (Root/Driving Number)**: the digital root of the birth day-of-month only.
- **Bhagyank (Destiny/Life Path Number)**: the digital root of the complete birth date, summing all digits of DD, MM, and YYYY together before reduction.
- **Master-number handling**: numbers 11, 22 (and, in some traditions, 33) are traditionally not reduced further; whether master-number retention is enabled is an explicit configuration value, since this varies by tradition and must not be silently assumed either way.
- **Compound-number handling**: the intermediate two-digit sum reached before final single-digit reduction may carry its own interpretive meaning in some traditions (compound-number tables); both the intermediate compound number and the final reduced digit must be retained and exposed, not just the final digit.
- **Spelling/name-change handling**: name numerology depends on the exact spelling used at calculation time; the exact name string used must be recorded as an explicit input, and a different spelling must trigger a fresh calculation rather than silently reusing a stale result.
- **Reduction rules**: standard digital-root reduction (repeatedly sum digits until a single digit remains), except for retained master numbers per the configuration above.
- **Source/tradition**: every numerology result is tagged with which system (Vedic/Chaldean vs. Pythagorean) produced it.
- **Versioning**: numerology configuration follows the same reproducibility/versioning requirement as astrology calculation configuration (see §Reproducibility).

## Combustion standard

Defines the standard and contract that **Phase 4 (Astronomical Calculation Engine)** implements. Locked per explicit project-owner decision (see §Versioning change log).

Combustion (Asta) is a derived astrological status, not raw ephemeris output: a planet is combust when its angular separation from the Sun (computed from each body's precise geocentric ecliptic longitude) is within that planet's specific orb threshold. Per-planet thresholds (degrees of separation from the Sun), per the Brihat Parashara Hora Shastra tradition as commonly implemented in mainstream Vedic astrology software:

| Planet | Threshold (direct) | Threshold (retrograde) |
|---|---|---|
| Moon | 12° | — |
| Mars | 17° | — |
| Mercury | 14° | 12° |
| Jupiter | 11° | — |
| Venus | 10° | 8° |
| Saturn | 15° | — |

The Sun itself is never combust. Rahu/Ketu are not evaluated for combustion under this standard (they are not physical bodies with a solar-separation relationship in the same sense).

Requirements:
- Combustion is computed as a **derived layer over raw longitudes**, not embedded inside ephemeris retrieval — see `docs/ARCHITECTURE.md` §"Astrology Engine Architecture" and the Phase 4 ephemeris-adapter/derived-status separation.
- The angular separation used is the absolute difference between the planet's and Sun's geocentric ecliptic longitude, normalized to the [0°, 180°] range (shortest angular distance).
- The retrograde-specific threshold (Mercury, Venus) applies only when that planet's own retrograde status (per its documented speed-sign semantics) is true at the same instant.
- The exact separation value and the threshold applied must be preserved alongside the boolean combust/not-combust result, for reproducibility and later rule-engine use.
- This threshold table is itself a versioned configuration value (§Reproducibility) — a future alternate combustion standard (e.g., a different classical source) must be a distinct, explicit configuration, never silently substituted.

## Node convention (Rahu / Ketu)

Defines the standard and contract that **Phase 4 (Astronomical Calculation Engine)** implements. Locked per explicit project-owner decision (see §Versioning change log).

Both **Mean Node** and **True Node** conventions are supported as an explicit, distinct `calculation_config` value — never silently mixed or substituted for each other. **Mean Node is the default** for the Vedic profile (§Default Vedic profile), matching the traditional convention used in most classical Parashari/Panchang software. True Node remains available as an explicit alternate configuration (anticipating KP's typical preference, `Phases.md` Phase 9).

- **Mean Node**: the smoothly regressing average node position (Swiss Ephemeris `SE_MEAN_NODE`).
- **True Node**: the actual perturbed node position, which can briefly station/go direct (Swiss Ephemeris `SE_TRUE_NODE`).
- **Ketu** is always derived as Rahu's longitude + 180°, under whichever node convention Rahu was computed with — never independently calculated via a second method.
- The node convention used must be recorded in the calculation configuration alongside ayanamsa/zodiac/house-system (§Reproducibility), so a result is always traceable to which convention produced it.

## Planetary aspects standard

Defines the standard and contract that **Phase 5 (Birth Chart / Kundli Engine)** implements. Locked per explicit project-owner decision (see §Versioning change log).

Pandit Ji implements classical Vedic **graha drishti** (sign/house-based planetary aspect), not the Western degree-based aspect system (conjunction/sextile/square/trine/opposition) — those remain out of scope for Phase 5.

- **All nine grahas** (Sun, Moon, Mars, Mercury, Jupiter, Venus, Saturn, Rahu, Ketu) cast the universal **7th-house/7th-sign aspect** — full-strength aspect on the sign/house directly opposite their own placement.
- **Mars** additionally aspects the **4th and 8th** signs/houses from its own placement.
- **Jupiter** additionally aspects the **5th and 9th** signs/houses from its own placement.
- **Saturn** additionally aspects the **3rd and 10th** signs/houses from its own placement.
- **Rahu and Ketu** cast only the standard 7th-house/sign aspect — **no special extra aspects** are attributed to them under this standard (unlike Mars/Jupiter/Saturn). This is an explicit locked decision, not an omission: some regional traditions attribute Mars-like extra aspects to Rahu/Ketu, but that variant convention is not adopted here.
- Aspects are evaluated sign-to-sign (whole-sign house convention, §Default Vedic profile) — a planet aspects every planet and every house placed in the aspected sign, not a degree-precise partial aspect.
- This aspect table is a versioned configuration value (§Reproducibility); an alternate aspect convention (e.g., Rahu/Ketu-as-Mars-like, or Western degree-based aspects) must be a distinct, explicit configuration, never silently substituted or blended with this one.

## Planetary dignity standard

Defines the standard and contract that **Phase 5 (Birth Chart / Kundli Engine)** implements. Locked per explicit project-owner decision (see §Versioning change log).

Dignity is evaluated from each planet's precise sidereal D1 longitude against the classical exaltation/debilitation degree and own-sign table below. **Mooltrikona is explicitly out of scope for Phase 5** — a documented deferral, not a silent omission; dignity states are limited to exalted/debilitated/own-sign/neutral.


**v1.4.0 note**: Moolatrikona remains outside this Phase 5 dignity model. Phase 6 derives a separate Moolatrikona fact from sign and degree (§Phase 6 rule-engine methodology); this table is unchanged.
| Planet | Exaltation (exact degree) | Debilitation (exact degree) | Own sign(s) |
|---|---|---|---|
| Sun | 10° Aries | 10° Libra | Leo |
| Moon | 3° Taurus | 3° Scorpio | Cancer |
| Mars | 28° Capricorn | 28° Cancer | Aries, Scorpio |
| Mercury | 15° Virgo | 15° Pisces | Gemini, Virgo |
| Jupiter | 5° Cancer | 5° Capricorn | Sagittarius, Pisces |
| Venus | 27° Pisces | 27° Virgo | Taurus, Libra |
| Saturn | 20° Libra | 20° Aries | Capricorn, Aquarius |

- **Exalted**: the planet's sign matches its exaltation sign (the exact degree is recorded for reference/strength-grading use by later phases, but sign-level exaltation does not itself require the exact degree).
- **Debilitated**: the planet's sign matches its debilitation sign.
- **Own sign**: the planet's sign is one of its own sign(s) above.
- **Neutral**: none of the above apply.
- Rahu/Ketu are not evaluated against this table under this standard (no classical exaltation/debilitation degree consensus is asserted here for the nodes; a future explicit addition, never silently assumed).
- This table is a versioned configuration value (§Reproducibility); Mooltrikona ranges, if added in a future phase, must be a distinct, explicit addition rather than silently merged into "own sign."

## Sign / House lordship standard

Defines the standard and contract that **Phase 5 (Birth Chart / Kundli Engine)** implements. This is the traditional, universally-agreed rulership table (no tradition-fork exists for it), recorded here as a versioned, citable standard rather than left as an implicit assumption inside code, per the same reproducibility discipline applied to every other standard in this document.

| Sign | Ruling planet |
|---|---|
| Aries | Mars |
| Taurus | Venus |
| Gemini | Mercury |
| Cancer | Moon |
| Leo | Sun |
| Virgo | Mercury |
| Libra | Venus |
| Scorpio | Mars |
| Sagittarius | Jupiter |
| Capricorn | Saturn |
| Aquarius | Saturn |
| Pisces | Jupiter |

- A house's "lord" (house-lord) is the ruling planet of the sign occupying that house, under the locked whole-sign house convention (§Default Vedic profile) — house lordship is therefore fully derived from sign lordship plus the house-to-sign mapping, not a separately defined concept.
- This table applies uniformly across D1 and every divisional chart (§Divisional charts (Vargas)): a chart's house/sign lords are always determined from that chart's own sign placements using this same table.
- Rahu/Ketu are never assigned sign rulership under classical Parashari convention and are excluded from this table; that exclusion is intentional, not an omission.

## Chalit / Bhava-Chalit and Ashtakvarga — explicitly out of scope for Phase 5

Both are recorded here as deliberate scope exclusions, not silent omissions or guesses:

- **Chalit (Bhava-Chalit) chart**: the alternate house-cusp-based bhava boundary system (as distinct from the locked whole-sign house convention) is deferred to a follow-up decision, per explicit project-owner direction. Phase 5 implements only the whole-sign Bhava convention already locked in §Default Vedic profile.
- **Ashtakvarga**: the Bindu/point-based strength-scoring system across all planets and houses was out of scope for Phase 5 per `Phases.md`'s explicit phase boundary (not listed among Phase 5's deliverables). It is implemented as Phase 9 WP-A1 — see §Ashtakavarga standards below.

## Ashtakavarga standards (Phase 9 WP-A1 methodology lock, v1.7.0; extended by WP-A2/A3, v1.8.0)

Scope: Bhinnashtakavarga (per-planet, 12-sign benefic-contributor counts), Sarvashtakavarga (their sum across the seven planet charts), and -- BPHS profiles only -- Trikona Shodhana, Ekadhipatya Shodhana and Pinda Sadhana (Rasi/Graha/Yoga Pinda) as structural facts only. No interpretation, verdict, remedy, strength label ("strong"/"weak") or narration. Out of scope, deferred, not silently dropped: Trikona/Ekadhipatya Shodhana and Pinda Sadhana under Brihat Jataka or Phaladeepika (no source read gives either a reduction procedure at all); BPHS Ch. 71's Ashtakavarga-based longevity (a lifespan/death-timing calculation, blocked under the product's Ayurdaya policy); BPHS Ch. 70 and 72's interpretive effect-judgments; any use of Ashtakavarga by Shadbala's Drik Bala or by Gochara/transits (Phase 8, which explicitly excluded Ashtakavarga scoring); Rahu and Ketu, which are never Ashtakavarga contributors (they are used only for the Ekadhipatya occupancy test, never as bindu contributors), because no source read (Brihat Jataka Ch. IX, Phaladeepika Ch. XXIII, BPHS Ch. 66) gives either node a table.

- **AV-01 (contributors and charts)**: eight contributors (the seven classical planets and the Ascendant) to each of seven Bhinnashtakavarga planet charts. BPHS additionally gives the Ascendant its own eighth chart (Ch. 66 v. 65-68); Brihat Jataka and Phaladeepika are silent on it (not excluding it), so it is present only under the two BPHS profiles and is never synthesized for the other two (`AshtakavargaFacts.lagna_chart` is `None` there).
- **AV-02 (four profiles, no default, no unqualified "BPHS")**: `ASHTAKAVARGA_BRIHAT_JATAKA_SASTRI_IX_1_7`, `ASHTAKAVARGA_PHALADEEPIKA_SASTRI_XXIII_3_9`, `ASHTAKAVARGA_BPHS_GRID_KAPOOR_66`, `ASHTAKAVARGA_BPHS_VERSE_KAPOOR_66`. The last two exist as separate profiles, rather than one "BPHS" profile, because BPHS's own printed dot grid and its own printed verse-translation list disagree with each other on 5 of 56 cells inside the same printed chapter (Ch. 66), and no basis exists, without Sanskrit review, to prefer one printed representation over the other.
- **AV-03 (terminology)**: BPHS's own text calls the *inauspicious* houses "Karanaprada" (dot-marked) and the *auspicious* ones "Rekhaprada" (line-marked) — the reverse of the popular modern shorthand "bindu = benefic point". Every profile is stored and reported as the houses each contributor considers **benefic** directly (the sense Brihat Jataka and Phaladeepika state outright, and BPHS's dot lists after taking the complement); the derived per-sign count is always called `benefic_count`, never "bindu count".
- **AV-04 (source-comparison register, 9 of 56 cells, no winner chosen)**: a complete, page-image-verified, cell-by-cell comparison of all four readings found 47 of 56 (chart, contributor) cells identical across all four and 9 differing, classified as: 2 cross-source variants (Brihat Jataka and Phaladeepika disagree; BPHS's own grid and verse agree with each other and side with one of the two), 2 BPHS-internally-consistent variants (Brihat Jataka and Phaladeepika agree; BPHS's own grid and verse agree with each other but differ from both), 2 BPHS-grid-only anomalies (the other three readings agree; only BPHS's printed dot grid differs), and 3 BPHS-verse-only anomalies (the other three readings agree; only BPHS's printed verse-translation list differs). Two cells carry a translator's footnote naming an alternate reading by another author (Varahamihira at the Moon chart's Jupiter cell, Parasara at the Venus chart's Mars cell); the footnote is recorded as `translator_note` and never merged into either table. The complete register is `pandit_astro_engine.ashtakavarga.constants.CROSS_TABLE_CONFLICTS`, machine-readable and independently reproduced in `services/astro-engine/tests/fixtures/ashtakavarga_source_comparison.json`.
- **AV-05 (effectless cells)**: Brihat Jataka Ch. IX sl. 3 states the 10th house from the Moon is effectless (neither benefic nor malefic) in Mars's chart. This applies only under the Brihat Jataka profile; no other source read states an effectless cell anywhere.
- **AV-06 (invariants)**: each source's own stated per-chart total (Sun 48, Moon 49, Mars 39, Mercury 54, Jupiter 56, Venus 52, Saturn 39; grand total 337) is reproduced independently by Brihat Jataka, Phaladeepika and BPHS's printed dot grid. BPHS's own printed verse-translation list sums to 340 (its three verse-only anomalies each add one extra benefic house relative to its own grid); this is reported, not corrected. The Ascendant's own BPHS chart totals 49.
- **AV-07 (validation)**: Brihat Jataka's own worked example (Ch. IX sl. 8 / Ch. VII sl. 6, printed p. 205) is reproduced exactly by the calculator across all 12 signs, including the effectless cell and the printed 39-benefic/56-malefic chart total — an independently source-published fixture, not generated by the implementation under test.
- **AV-08 (statuses and reason codes)**: `AshtakavargaStatus` (SUCCESS, NOT_EVALUABLE, INVALID_INPUT, UNSUPPORTED_PROFILE, INTERNAL_ERROR) and `AshtakavargaReason` (`lagna_unavailable` when the Ascendant longitude is missing — mandatory as a contributor under every profile; `natal_positions_incomplete` when any of the seven planets is missing; `non_finite_longitude`; `unsupported_profile`). A failed result carries no chart, Sarva or Lagna-chart facts.
- **AV-09 (WP-A2/A3 scope, BPHS profiles only)**: Trikona Shodhana, Ekadhipatya Shodhana and Pinda Sadhana are BPHS Ch. 67-69 (Kapoor) only; no reduction is defined for, or applied to, Brihat Jataka or Phaladeepika. A reduction request under either of those two profiles returns `NOT_EVALUABLE(reduction_unsupported_for_profile)`, never a silently computed result. Reductions are modelled as a separate, additive request/response pair (`AshtakavargaReductionRequest` / `AshtakavargaReductionFacts`) so the WP-A1 contract (`AshtakavargaRequest` / `AshtakavargaFacts`) is completely unchanged.
- **AV-10 (Trikona and Ekadhipatya Shodhana)**: Trikona Shodhana (Ch. 67 v. 3-5) subtracts the minimum of each trine group's three values from all three; verified exactly against BPHS's own worked example (Ch. 67 pp. 868-869) on all 12 signs. Ekadhipatya Shodhana (Ch. 68 rules 1-6) applies only to the five planets owning two signs each (Mars, Venus, Mercury, Jupiter, Saturn; Sun and Moon's single signs are never touched) and resolves three cases: both signs occupied -> unchanged (rule 2, for any values, even equal); neither occupied -> both reduced by the pair's minimum (rules 1/5, unified, unambiguous at every relative size including equal); exactly one occupied, unequal -> the occupied sign stays unchanged and the empty sign becomes `max(0, empty − occupied)` (rules 3/4, unified). Occupancy for this test includes Rahu and Ketu (confirmed by the chapter's continued example: Sagittarius, occupied only by Ketu, is treated as "with a planet") but never the Ascendant alone (an explicit, documented inference — no worked example isolates this case).
  **A fourth case is deliberately left `NOT_EVALUABLE`, not resolved**: exactly one sign occupied AND the two Trikona-corrected values equal. BPHS's own "abstract illustration" (Ch. 68 p. 876) answers this identical shape two different ways within the same printed table: the Capricorn/Aquarius pair keeps the occupied sign unchanged (matching rule 6's literal English, "the number of the *latter* [without-planet sign] should be reduced to zero"), while the Gemini/Virgo pair, under the same shape, prints as if *both* signs were zeroed. Because a genuine, source-internal contradiction exists at this exact rule shape, this case returns `NOT_EVALUABLE(ekadhipatya_equal_value_conflict)` for the affected chart rather than silently choosing either printed reading. Both readings are preserved verbatim, with page-level provenance, on `EkadhipatyaConflict` (`reading_a_occupied_value`, matching Capricorn/Aquarius; `reading_b_occupied_value`, matching Gemini/Virgo). Because Rasi Pinda sums all 12 signs, an unresolved sign makes the *whole chart's* Rasi, Graha and Yoga Pinda `NOT_EVALUABLE` too (`ChartReduction.ekadhipatya_corrected` and `PindaResult`'s Pinda fields are withheld entirely, never partially populated around the disputed pair) — see AV-13.
- **AV-11 (Pinda Sadhana: Rasi and Yoga Pinda)**: Rasi Pinda (Ch. 69 v. 1-2) sums, over all 12 signs, the Ekadhipatya-corrected value times that sign's Rasimana multiplier (Aries 7, Taurus 10, Gemini 8, Cancer 4, Leo 10, Virgo 6, Libra 7, Scorpio 8, Sagittarius 9, Capricorn 5, Aquarius 11, Pisces 12 — verse and table agree, no conflict). Yoga Pinda = Rasi Pinda + Graha Pinda (stated directly in the source). Both verified exactly against the chapter's own worked example (Rasi Pinda 100, Yoga Pinda 148).
- **AV-12 (Pinda Sadhana: Graha Pinda, one resolved and one open source conflict)**: Graha Pinda sums, over signs occupied by exactly one classical (non-Lagna, non-node) planet, the Ekadhipatya-corrected value times that planet's Grahamana multiplier. **Resolved by worked-example arithmetic, not by preference**: the verse (v. 1-4) states 6 for Sun, Moon and Saturn, but the printed Grahamana Chakra table states 5 for each; substituting the verse's "6" into the chapter's own worked example gives a Graha Pinda of 56, not the source's own stated 48 — only the table's "5" reproduces 48 exactly, so 5 is used for Sun, Moon and Saturn and the verse's "6" is treated as a printing/translation error. Jupiter (10) and Venus (7) agree between verse and table (untested by the example, since both only ever co-occupy a zero-valued sign in it). **Open, not resolved**: Mercury's multiplier (table 5, verse 6) is never isolated by any worked example; a sign occupied solely by Mercury with a nonzero value makes that chart's Graha Pinda (and therefore Yoga Pinda) `NOT_EVALUABLE(mercury_multiplier_conflict)` rather than silently choosing 5 or 6. A sign occupied by more than one classical planet at once is likewise `NOT_EVALUABLE(multiple_occupants_unsupported)` when nonzero (no source read states an aggregation rule); a zero-valued sign never triggers either case, matching the source's own worked example (which silently skips a four-occupant, zero-valued Aquarius without comment).
- **AV-13 (Lagna's own Pinda; failure propagation)**: the Ascendant's own BPHS chart (AV-01) goes through the identical Trikona, Ekadhipatya and Pinda pipeline as any planet's chart (explicit in Ch. 67 v. 1-2, "including the Ascendant"); it has no lordship pair of its own (the Ascendant does not own a sign) but its per-sign values still participate in other planets' Ekadhipatya occupancy tests exactly as before, and it is just as subject to the AV-10 equal-value conflict as any planet chart (confirmed: a real natal chart used for the WP-A2/A3 test fixture triggers exactly this conflict on the Ascendant's own Scorpio/Aries pair). Each of the eight possible charts (seven planets plus the Ascendant) resolves this independently — one chart's conflict does not affect another's, or the overall request's `status`, which stays `SUCCESS` as long as the request itself was valid.

Affected calculations: new only (Ashtakavarga Bhinna and Sarva facts in v1.7.0; Trikona/Ekadhipatya Shodhana and Pinda Sadhana facts in v1.8.0). No Phase 4, 5, 6, 7 or 8 formula changed, and the WP-A1 `AshtakavargaFacts` contract is unchanged by v1.8.0. Affected interpretations: none; later phases consume the facts. Regression-test requirement: the full 56-cell source-comparison fixture and 9-cell conflict register, the Brihat Jataka worked-example fixture, rotation-arithmetic and invariant tests, per-profile Lagna-chart presence/absence tests, every failure path, the Ch. 67/68/69 continued worked-example fixture (Trikona, Ekadhipatya, Rasi/Graha/Yoga Pinda), the Ch. 68 abstract-illustration cases, and the Mercury-conflict / multi-occupant / zero-value edge cases (`services/astro-engine/tests/test_ashtakavarga_*`). Approval status: the WP-A1 four-profile design was approved by the project owner at Phase 9 Gate 1 following an independent reconciliation audit; the WP-A2/A3 methodology (the consolidated reduction rules, the worked-example-proven Graha multipliers, the Mercury conflict left unresolved, the BPHS-only scope, and the additive request/response design) was approved by the project owner following a dedicated WP-A2/A3 research and methodology report.

## Data & privacy principles

Product-level data/privacy principles (data minimization, purpose limitation, consent/notice, birth-data/location-data/conversation-data/palm-image handling, retention/deletion, access control, encryption, auditability, personalization-vs-training-data separation, third-party/vendor restrictions, user export/deletion, minor/child safeguards) are defined in `PRODUCT_POLICIES.md` §"Data & Privacy Principles" — this document cross-references rather than duplicates that content, per the source-of-truth hierarchy (`SOURCE_OF_TRUTH.md`). `LEGAL_REGULATIONS.md` remains the detailed legal/compliance baseline underneath both.

## Prediction language policy

- Clearly distinguish three claim classes at all times (matching `SOURCE_OF_TRUTH.md`'s claim-class taxonomy): **calculation facts** (deterministically computed), **traditional interpretations** (what a named tradition/school teaches, per §Yoga/Dosha standards' source-tagging), and **empirical/scientific evidence** (measured, backtested performance — see `docs/ARCHITECTURE.md` §11 and `research/BACKTESTING.md`). Never blur these into one undifferentiated claim.
- Never present a guaranteed future outcome.
- Never claim "100% accurate prediction" or equivalent unqualified certainty.
- Communicate uncertainty wherever it materially affects the interpretation (see §Birth-time uncertainty and §Uncertainty below).
- Never use fear-based framing to manufacture certainty or urgency.
- Never present astrology as a substitute for professional medical, legal, or financial advice (see §Health, and `PRODUCT_POLICIES.md`'s High-impact topics section).

## Interpretation
Every major conclusion should be traceable to:
1. calculation facts
2. applicable rule/source
3. relevant period
4. supporting factors
5. conflicting factors
6. evidence/confidence state

Clearly separate calculated fact, traditional interpretation, AI explanation, and user-provided information.

## Birth-time uncertainty
Birth-time precision materially affects some calculations (house cusps, exact Nakshatra/pada boundaries near a transition, Dasha balance). If a user provides an approximate time (e.g., "around 5 PM," "between 4-6 PM," "unknown"), that uncertainty must be preserved through the pipeline and reflected in the response — never silently converted into a false-precision exact timestamp. Future birth-time rectification functionality may be supported as its own explicitly-labeled methodology, but its output must never be presented as certainty.

## Uncertainty
Approximate birth time must remain approximate. Do not silently treat it as exact.

No scientifically unsupported guarantee of future prediction.

## Reproducibility
Store engine version, ephemeris/version, ayanamsa, house system, timezone database version, coordinates, and birth inputs with calculation results.

## Health
Astrological health content is traditional/educational only, never diagnosis, prognosis, treatment or emergency advice.

## Remedies
Gemstones, mantras, puja, fasting, donations and rituals are traditional/spiritual practices, not scientifically established treatments.

## Palmistry
Image quality, hand side, visible structures and uncertainty must be separated from interpretation. No medical diagnosis from palm images. AI Palm Reading is a locked product feature (`features.md` §34), implemented in `Phases.md` Phase 13 (Palm Reading & Vision Intelligence); this standard applies to that implementation.

## Versioning
Changes to calculation standards require versioning, changelog, regression tests and explicit approval. A standards change record must include: version number, date, change description, reason, affected calculations, affected interpretations, regression-test requirement, and approval status. Historical calculation behavior must never be silently altered — a standards version change is itself a new, distinct, recorded configuration state.

### Change log
- **v1.0.0** (Pre-Phase-1 Foundation package): initial draft — Core rule, Default Vedic profile, Supported systems (list only), Interpretation, Uncertainty, Reproducibility, Health, Remedies, Palmistry, Versioning.
- **v1.1.0** (Phase 1 completion): added AI scope boundary; expanded Time/Location standards; expanded Supported systems into full per-system methodology standards (Vedic, Western/Tropical, KP, Lal Kitab, Nadi, with Lal Kitab/Nadi explicitly marked provisional pending research validation); added Divisional charts (Vargas), Vimshottari Dasha, Nakshatra standards, Yoga/Dosha standards, Panchang/Muhurta standards, and Numerology standards sections; added Data & privacy principles (cross-reference to `PRODUCT_POLICIES.md`); added Prediction language policy; added Birth-time uncertainty section. Approval status: locked per project-owner direction to close all 13 `Phases.md` Phase 1 content requirements.
- **v1.2.0** (Phase 4 pre-implementation lock): added Combustion standard (per-planet orb thresholds, Brihat Parashara Hora Shastra tradition) and Node convention (Rahu/Ketu: Mean Node default, True Node supported as explicit alternate config) — both were confirmed genuinely unspecified during Phase 4's mandatory cross-check and required an explicit project-owner decision before the astronomical calculation engine could implement them. Reason: `Phases.md` Phase 4 requires combustion and Rahu/Ketu as deliverables; no prior version of this document defined either sufficiently to implement without guessing. Affected calculations: Phase 4's combustion status and node/Ketu derivation. Affected interpretations: none yet (Phase 6 rule content will consume these facts later). Regression-test requirement: Phase 4's golden/boundary tests must cover both thresholds and both node conventions. Approval status: locked per explicit project-owner decision.
- **v1.3.0** (Phase 5 pre-implementation lock): expanded §Divisional charts (Vargas) with full derivation formulas for the 14 remaining Shodashvarga charts (D2, D3, D4, D7, D10, D12, D16, D20, D24, D27, D30, D40, D45, D60), completing the locked 16-varga set; added Planetary aspects standard (Vedic graha drishti — universal 7th aspect for all nine grahas, Mars 4th/8th, Jupiter 5th/9th, Saturn 3rd/10th, no special extra aspects for Rahu/Ketu); added Planetary dignity standard (exact exaltation/debilitation degrees and own-sign table for the seven classical grahas, Mooltrikona explicitly excluded); added Sign/House lordship standard (the traditional 12-sign ruler table); recorded Chalit/Bhava-Chalit and Ashtakvarga as explicit out-of-scope deferrals for Phase 5. Reason: `Phases.md` Phase 5 requires house lords, planetary aspects, planetary dignity, and all sixteen divisional charts as deliverables; Phase 5's mandatory 35-item pre-implementation standards audit confirmed only D1/D9 had derivation formulas and aspects/dignity/lordship had no locked standard, so all were genuinely unspecified and required explicit project-owner decisions before the Kundli engine could implement them. Affected calculations: Phase 5's house-lord, aspect, dignity, and D2/D3/D4/D7/D10/D12/D16/D20/D24/D27/D30/D40/D45/D60 chart derivations. Affected interpretations: none yet (later rule-engine/interpretation phases will consume these facts). Regression-test requirement: Phase 5's boundary/golden/invariant tests must cover every varga formula's sign-boundary transitions, every aspect rule, and every dignity degree threshold. Approval status: locked per explicit project-owner decision (two `AskUserQuestion` rounds: aspects/dignity-scope/varga-research-approach/Chalit-deferral, then D3-rule-choice and overall-lock-confirmation).
- **v1.4.0** (Phase 6 pre-implementation lock): added §Phase 6 rule-engine methodology (source tiers; source-profile naming and rule identifiers; ambiguity-preserving evaluation; result statuses and reason codes; aspect refinement for "aspected"/"unaspected" conditions; strength dependency on Shadbala; BPHS Ch. 3 natural and compound relationships, natural benefic/malefic with two labelled Pandit Ji conventions, and the BPHS Ch. 34 84-cell functional-nature table as the canonical BPHS functional profile; node policy; Moolatrikona as a Phase 6 fact; D27 recorded as UNRESOLVED with the Phase 5 D27 formula unchanged; dependencies owned by other phases; evidence bundle contents) and clarified that Vimshottari balance and year-length conventions are Phase 7 decisions. Reason: `Phases.md` Phase 6 requires a versioned rule engine, and the Phase 6 methodology research (`research/ASTROLOGY_SOURCES.md` Groups 1–8) found relationships, natural and functional nature, strength, aspect refinement, node treatment, Moolatrikona and ambiguity handling genuinely unspecified. Affected calculations: none in Phases 4–5 (no formula changed). Affected interpretations: all Phase 6 rule outputs. Regression-test requirement: Phase 5 tests must pass unchanged; Phase 6 tests must cover every rule family. Also added §Language and multilingual interaction (AI understands English, Hindi, Hinglish, Romanized Hindi and code-switching regardless of UI language; no AI language toggle; UI language English or हिन्दी set in Account → Language, default English, persisted, independent of AI response language, which is defined in the Agent phase). Phase 5 calculation records keep `standards_version = 1.3.0`. Approval status: approved by the project owner, Phase 6 Group 8 decision.
- **v1.4.1** (Nakshatra/Pada boundary-convention clarification, 2026-09-20): §Nakshatra standards now records the half-open, lower-inclusive/upper-exclusive classification convention for Nakshatras and Padas, exact rational classification of the normalized float, and 360° = 0°, labelled a Pandit Ji engineering convention and not a classical source rule; the existing `near_boundary` behaviour and the tiny-negative-float normalization behaviour are recorded as unchanged. Reason: Phase 5's `nakshatra_position()` divided by the float 360.0/27.0 and assigned exactly representable boundary longitudes to the lower bucket (40.0° returned Krittika, 10.0° returned Pada 3); the defect was reproduced, and the project owner approved the convention and the correction. Versioning note: a patch-level increment was used because no formula, table or classical value changed, only the previously unstated side of a boundary; v1.5.0 remains available for the Phase 7 pre-implementation lock. Affected calculations: the Nakshatra and Pada of any longitude lying exactly on a boundary (implemented in commit `091ca1b`, CI run `35500359836`); all other longitudes classify identically to before (0 differences over 200,000 random longitudes, compared old versus new). Affected interpretations: any later consumer of Nakshatra, Pada or the Vimshottari starting lord for such a longitude; none yet, because no Phase 7 code exists. Regression-test requirement: exact-boundary, neighbourhood, normalization and Kundli-path tests (added in `091ca1b`; astro-engine 353 tests, rule-engine 235 tests). Open follow-up: Phase 5 calculation records still carry `standards_version = 1.3.0` (`kundli.py` `STANDARDS_VERSION`), although their boundary behaviour now follows v1.4.1; whether and how that constant advances is an owner decision and a code change, not made here. Approval status: the convention and the code correction were approved by the project owner; this documentation entry records that approval.
- **v1.5.0** (Phase 7 methodology lock, 2026-09-20): added §Phase 7 methodology lock to §Vimshottari Dasha: scope and exclusions (Vimshottari to the Pratyantar level; facts only, no interpretation; Maraka timing is a later rule-engine consumer; no Sookshma, Prana, other Dasha systems or rectification); the balance-at-birth default profile `DASHA_STANDARD_V1_BALANCE_LONGITUDE` with the two source-alternative profiles documented and inactive; the year-length default profile `YEAR_365_2425_FIXED_DAY` with alternatives; the sub-period profile `DASHA_SUBPERIOD_PROPORTIONAL_FULL_PARENT_V1`; UTC and exact-arithmetic time policy; the half-open boundary convention for Dasha periods; the EXACT, APPROXIMATE and NOT_EVALUABLE birth-time precision model; statuses and reason codes; the `DashaFacts` contract and the Phase 6 evidence-bundle integration. Reason: `Phases.md` Phase 7 requires Vimshottari Dasha; the balance method is a documented source conflict, no verse read states the year length, and the treatment of the birth-balance Mahadasha's sub-periods is not stated in the sources read, so each needed an explicit, labelled engineering profile before implementation. Every profile is a Pandit Ji engineering convention or a source-supported statement at its stated verification level, never presented as the only valid classical method. Affected calculations: new only (Vimshottari periods); no Phase 4, 5 or 6 formula changed. The Phase 5 `STANDARDS_VERSION` constant deliberately stays 1.3.0 (see the lock's version note). Affected interpretations: none; later phases consume the facts. Regression-test requirement: sequence and years, balance, generation, boundaries, precision, lookup, serialization and bundle-integration tests (astro-engine `test_dasha_*`, rule-engine `test_dasha_*`). Open items recorded for the owner: (1) confirmation of the full-parent treatment of the birth-balance Mahadasha's sub-periods; (2) the balance-method source conflict remains open by design and can be revisited if the Panchanga-time profile is implemented. Approval status: methodology defaults and scope approved by the project owner in the Phase 7 implementation directive; the sub-period treatment and the retained Kundli constant are implementation decisions recorded here for owner review.
- **v1.6.0** (Phase 8 methodology lock, 2026-09-21): added §Transit / Gochar standards (TR-01 to TR-15): scope and exclusions (transit facts and provenance only; no interpretation, remedies, alerts, Ashtakavarga scoring, degree or orb contacts, Dhaiya, Ashtama or degree-based Sade Sati); the `TRANSIT_REF_MOON_SIGN` default reference and the optional engineering-convention Lagna fact; the favourable-house readings of Phaladeepika, Brihat Samhita, Brihat Jataka and a BPHS-derived reading, with the Moon-from-Moon conflict preserved as `NOT_EVALUABLE(reading_ambiguous)`; the single-source Rahu and Ketu treatment; the Phaladeepika-specific Vedha profile with its exceptions, the node-occupant rule and the Venus translation anomaly; sign-based transit-to-natal contacts; the engineering-convention event kinds; the `modern_tradition` Sade Sati profile with segments and episodes; the UTC, half-open boundary, solver-tolerance and accuracy policy with the JPL Horizons engineering evidence and the ayanamsa-sensitivity note; the 200-year and 50,000-event limits; statuses and reason codes; the `TransitFacts` contract and the additive evidence-bundle section. Reason: `Phases.md` Phase 8 lists transits and Sade Sati but stated no dependencies, methodology, exclusions or exit criteria, and the sources read give a Gochar scheme with Vedha only in Phaladeepika and agree across texts only partly (the Moon from the Moon conflicts; the nodes are single-source; Sade Sati as a combined unit is not classical). Affected calculations: new only (transit states, events, Sade Sati segments); no Phase 4, 5, 6 or 7 formula changed (an additive node-ID constant was added to the ephemeris adapter). Affected interpretations: none; later phases consume the facts. Regression-test requirement: exact synthetic-motion ingress, station and retrograde re-entry tests; source-reading, Vedha, node and conflict tests; window, limit and precision tests; determinism and serialization; evidence-bundle integration; all earlier suites unchanged. Approval status: methodology approved by the project owner at Approval Gate 1 (all nine recommendations); the implementation, validation and CI verification are recorded in `SUMMARY.md` §26 (commits `27afb4d`, `6af6112`, `7cd8887`, `f846e7e`; CI run `35639087407` green); the commit was approved by the project owner at Approval Gate 2.
- **v1.7.0** (Phase 9 WP-A1 methodology lock, 2026-09-22): added §Ashtakavarga standards (AV-01 to AV-08): four independent, never-merged source profiles (Brihat Jataka, Phaladeepika, and BPHS's own printed dot grid and printed verse-translation list, which disagree with each other on 5 of 56 cells in the same chapter); the terminology note that BPHS's own "dot" marks the inauspicious houses (the reverse of the popular "bindu = benefic" shorthand), so this standard always reports `benefic_count` directly; the complete 56-cell, 9-conflict source-comparison register with no winner chosen and two translator-footnote attributions kept as `translator_note`; the Brihat Jataka effectless-cell exception; the 337/49 total invariants (and BPHS's own verse-translation list's internal 340 anomaly, reported not corrected); the Brihat Jataka worked-example validation fixture; and the `AshtakavargaFacts` contract. Reason: `Phases.md` Phase 9 lists Ashtakavarga (BPHS Ch. 66-72) as requiring a methodology audit before implementation; the audit found BPHS's own two printed representations of Ch. 66 (its dot grid and its verse translation) disagree with each other on 5 cells, and cross-checking against Brihat Jataka and Phaladeepika (each independently page-verified for the first time this phase, not merely OCR-checked) found a further 4 genuine cross-source variants, so a single unqualified "BPHS" profile could not be locked without silently picking a printed representation. Affected calculations: new only (Ashtakavarga Bhinna and Sarva facts); no Phase 4-8 formula changed. Affected interpretations: none yet. Regression-test requirement: the full 56-cell fixture and 9-cell conflict register, the Brihat Jataka worked example, rotation-arithmetic and invariant tests, per-profile Lagna-chart tests, and every failure path (astro-engine `test_ashtakavarga_*`, 818 tests total including 30 new). Approval status: the research-hardening findings (a complete independent reconciliation of every comparison count, resolving an internal inconsistency between an earlier "52/56" summary and the audited "50/56 three-way, 9/56 four-way" figures) and the four-profile design were reviewed and approved by the project owner across two Phase 9 Gate 1 follow-up rounds before this implementation.
- **v1.8.0** (Phase 9 WP-A2/A3 methodology lock, 2026-09-22): extended §Ashtakavarga standards with AV-09 to AV-13: Trikona Shodhana, Ekadhipatya Shodhana and Pinda Sadhana (Rasi, Graha and Yoga Pinda), scoped to the two BPHS profiles only (Brihat Jataka and Phaladeepika give no reduction procedure), modelled as an additive `AshtakavargaReductionRequest`/`AshtakavargaReductionFacts` pair that leaves the WP-A1 contract untouched. Trikona and Ekadhipatya Shodhana and Rasi/Yoga Pinda are verified exactly against BPHS's own worked examples (Ch. 67 pp. 868-869, Ch. 68 p. 876, Ch. 69 pp. 879-880). Graha Pinda's multiplier for Sun, Moon and Saturn (5, not the verse's stated 6) is resolved by direct arithmetic proof from the chapter's own stated total (48); Mercury's multiplier (table 5 vs verse 6) is left genuinely unresolved and returns `NOT_EVALUABLE(mercury_multiplier_conflict)` rather than guessed, as does a sign shared by more than one classical planet (`NOT_EVALUABLE(multiple_occupants_unsupported)`). A genuine internal inconsistency in the Ch. 68 abstract illustration (its Gemini/Virgo pair contradicts its own Capricorn/Aquarius pair under the identical rule 6 shape) is resolved conservatively, not by picking a side: the "exactly one occupied, equal values" case returns `NOT_EVALUABLE(ekadhipatya_equal_value_conflict)`, with both printed readings preserved verbatim and cited on `EkadhipatyaConflict`, and the conflict propagates to withhold that chart's entire Rasi, Graha and Yoga Pinda (never a partial total). Reason: `Phases.md` Phase 9's Ashtakavarga methodology audit explicitly named these three chapters as requiring page-image verification and a worked-example check before implementation, and BPHS Ch. 71 (Ashtakavarga-based longevity) and Ch. 70/72 (interpretive effect-judgments) were found and explicitly excluded, the former under the product's Ayurdaya policy. A focused review after the first implementation attempt found the equal-value case should not be silently resolved either way, given the source's own internal contradiction; the conservative `NOT_EVALUABLE` handling was adopted on the project owner's explicit direction. Affected calculations: new only; no Phase 4-8 or WP-A1 formula or contract changed. Affected interpretations: none. Regression-test requirement: the Ch. 67/68/69 continued worked-example fixture (including the Ascendant's own chart genuinely hitting this conflict), the Ch. 68 abstract-illustration cases (detection, reason code, provenance, isolation from unaffected pairs), the Mercury-conflict and multi-occupant edge cases, WP-A1 regression, determinism and JSON round trip (astro-engine `test_ashtakavarga_reductions.py`, `test_ashtakavarga_pinda.py`, `test_ashtakavarga_reduction_service.py`, 850 tests total including 32 new). Approval status: the WP-A2/A3 methodology report and the subsequent conservative-handling decision for the Ekadhipatya equal-value conflict were both reviewed and approved by the project owner before implementation.
- **v1.9.0** (Phase 9 WP-EB methodology lock, 2026-09-22): added AV-14 to AV-18, integrating the already-locked Ashtakavarga facts (WP-A1 `AshtakavargaFacts` and WP-A2/A3 `AshtakavargaReductionFacts`) into the rule-engine `EvidenceBundle` as one additive, optional `ashtakavarga` section, mirroring the Phase 7 Dasha and Phase 8 Transit integrations: the rule engine performs no astrology computation and only re-shapes and validates the astro-engine JSON it is given (enforced by the existing repository-wide source-purity test); the caller selects exactly one of the four AV-02 profiles per bundle build, with no implicit default and no silent multi-profile merge (a caller wanting more than one profile's evidence builds more than one bundle); every WP-A1/A2/A3 `NOT_EVALUABLE` status, reason code and `EkadhipatyaConflict` reading is preserved verbatim, never resolved or merged by the adapter, and a chart's reduction map or Pinda is copied only when astro-engine itself marks it resolved (the same all-or-nothing contract, never a partial "success"); the static AV-04 `CROSS_TABLE_CONFLICTS` cross-source register is build-time registry data and is never copied into a per-Kundli bundle. Reason: the project owner's Phase 9 EvidenceBundle integration directive required the WP-A1/A2/A3 facts to reach the evidence bundle before Jaimini (WP-B) implementation begins, following the existing architecture rather than a parallel design, with backward compatibility, deterministic canonical JSON/hashing and no astrology logic leaking into the rule engine as hard constraints. Affected calculations: none (no astro-engine formula, model or profile changed; `services/astro-engine` has an empty diff for this change). Affected interpretations: none; no shipped rule reads the new section yet. Regression-test requirement: per-profile export tests, profile-selection and invalid-input rejection tests, WP-A1/A2/A3 conflict-preservation tests (Mercury multiplier, multi-occupant, Ekadhipatya equal-value), provenance and validation-rejection tests, deterministic canonical JSON/hash and JSON round-trip tests, backward-compatibility tests proving an absent section reproduces the pre-existing bundle hash byte-for-byte, the rule-engine transport-boundary test, and real end-to-end astro-engine-to-bundle tests (rule-engine `test_ashtakavarga_evidence.py`, `test_ashtakavarga_integration.py`). Approval status: implemented per the project owner's Phase 1 EvidenceBundle integration directive; commit, push and CI verification are recorded separately once confirmed green (see `SUMMARY.md`).
- **v1.10.0** (Phase 9 WP-B-1 methodology lock, 2026-09-22): added JN-01 to JN-05, locking Rashi Drishti (BPHS Ch. 8 v. 1-3, Santhanam translation, Vol I, printed pp. 105-107): a static, longitude-independent sign-to-sign aspect table under profile `RASHI_DRISHTI_BPHS_8_1_3`, kept strictly separate from Phase 5/6's planet-to-house graha drishti. Reason: the project owner's Phase 2 WP-B directive required Rashi Drishti re-verified against the primary source (not reused from an earlier, less rigorous pass) before implementation; page-image verification found the rule attributed to Parasara rather than Jaimini (the translator's own note explains the popular "Jaimini" nickname), and found the chapter's own v. 4-5 extension of the same table to a planet's own placement, which is recorded but deliberately excluded because it would conflict with the already-locked graha drishti for the same placement. Affected calculations: new only (`pandit_astro_engine.jaimini.rashi_drishti`); no Phase 4-9 formula changed. Affected interpretations: none yet; no shipped rule reads Rashi Drishti facts. Regression-test requirement: all 12 signs against BPHS's own printed table, adjacent-sign exclusion for both movable and fixed signs, common-sign coverage, mutual-relation proof, determinism, invalid-sign rejection, and a regression proving no conflation with graha drishti (astro-engine `test_rashi_drishti.py`). Approval status: implemented per the project owner's Phase 2 WP-B-1 directive; commit, push and CI verification are recorded separately once confirmed green (see `SUMMARY.md`).
- **v1.11.0** (Phase 9 WP-B-2 methodology lock, 2026-09-22): added JN-06 to JN-10, locking Chara Karaka and Constant Karaka (BPHS Ch. 32 v. 1-21, Santhanam translation, Vol I, printed pp. 316-320). Reason: re-verifying the source at page-image level (per the project owner's "verify again, do not reuse prior research" directive) found BPHS's own v. 1-2 states three disagreeing positions on the Chara Karaka candidate-body scope rather than one settled "eight-body" position as originally assumed; reported to the project owner, who directed shipping two profiles (`JAIMINI_CHARA_KARAKA_SEVEN_BODY_BPHS_32_1_17`, `JAIMINI_CHARA_KARAKA_EIGHT_BODY_BPHS_32_1_17`) with no default, mirroring the Ashtakavarga precedent, and skipping the underspecified conditional third reading. The same re-verification found the source's own tie-handling differs by rank (v. 13's "deficit of one karaka, consider constant significator" for non-Atma roles versus v. 3-8's minutes/seconds tie-break, with v. 9-12's emphasis on Atma Karaka's unique importance motivating this lock's differentiated top-tie treatment); the project owner directed expanding WP-B-2 to also implement Ch. 32 v. 18-21 (Constant Karaka) so the source's real fallback is available, rather than only a synthetic `NOT_EVALUABLE`. Affected calculations: new only (`pandit_astro_engine.jaimini.chara_karaka`, `.constant_karaka`); no Phase 4-9 formula changed. Affected interpretations: none yet; no shipped rule reads either fact set. Regression-test requirement: the source's own worked "standard nativity" example (all 8 role-to-planet assignments reproduced exactly), Rahu's reverse-degree convention, highest/lowest comparison degree, exact degree/minute/second ties at the top (unresolved) and at a lower rank (shared role, deficit at the bottom), the 7-body profile's structural Dara Karaka deficit, Ketu exclusion, profile/input validation, determinism, and the Constant Karaka table's five resolved plus two `NOT_EVALUABLE(strength_undefined)` entries (astro-engine `test_chara_karaka.py`, `test_constant_karaka.py`). Approval status: the body-scope finding and the WP-B-2 scope expansion to include Constant Karaka were both put to the project owner and approved before implementation; commit, push and CI verification are recorded separately once confirmed green (see `SUMMARY.md`).
- **v1.12.0** (Phase 9 WP-C methodology lock, 2026-09-22): added PD-01 to PD-10, locking Partial/Degree Drishti as two separate, never-merged profiles: `PARTIAL_DEGREE_DRISHTI_BPHS_26` (BPHS Ch. 26, Santhanam translation, Sanskrit-cross-checked) and `PARTIAL_DEGREE_DRISHTI_UTTARAKALAMRITA_SRIPATI` (Uttara Kalamrita Ch. 2, citing "Sripatipaddhati-II", unverified). Reason: this is prerequisite groundwork for `Phases.md`'s "Shadbala — BPHS Ch. 27-28" line item (Ch. 27 v.19's Drik Bala explicitly aggregates Ch. 26's per-pair values) rather than a separately-named roadmap item; a multi-round research process found (1) BPHS's own general formula is mathematically identical to linear interpolation between its stated discrete checkpoints, a project-level derivation never to be presented as either source's own claim; (2) BPHS's literal reduction step, Sanskrit-confirmed as the numeral "10" (the bhūtasaṃkhyā word "dik"), correctly reproduces the house-8 checkpoint but contradicts BPHS's own stated house-9/house-10 pairings, a permanent, unresolved source-internal conflict, not a translation error; (3) a genuinely comparable second primary source (Uttara Kalamrita, citing Sripatipaddhati) gives a structurally different interpolation method with no such gap; (4) neither source specifies how Saturn/Mars/Jupiter's confirmed full-aspect peak values interact with the surrounding continuous curve, so no tolerance or blending rule is invented for either profile. Affected calculations: none yet -- methodology only, no code exists. Affected interpretations: none; `docs/ASTROLOGY_STANDARDS.md` line 307's existing `requires_partial_drishti` reason code (locked at Phase 6) is a known future consumer, not integrated by this lock. Regression-test requirement (for the future implementation): all twelve PD-02 checkpoints reproduced exactly by both profiles; the branch-formula-equals-interpolation equivalence verified computationally; house 8 asserted `SUCCESS` and houses 9-10 asserted `NOT_EVALUABLE(reduction_rule_conflict)` for Profile A; houses 9-10 asserted `SUCCESS` for Profile B; each special planet's exact peak asserted `SUCCESS=60` for Profile A only, with Profile B asserting `NOT_EVALUABLE(special_formula_interior_unresolved)` unconditionally including at the exact peak; non-conflation regression against `aspects.py` and `jaimini`; determinism. Approval status: the full methodology, including every conservative `NOT_EVALUABLE` handling decision, was reviewed and explicitly approved by the project owner across a multi-round research-and-revision process before this lock; implementation has not begun.
- **v1.13.0** (Phase 9 WP-C reason-code addendum, 2026-09-22): added PD-10 (renumbering the prior PD-10 "Versions" to PD-11), locking `aspecting_node_unresolved` as a third reason code, symmetric across both WP-C profiles. Reason: a focused source check (requested before implementation, since `CelestialBody.RAHU`/`.KETU` are structurally valid inputs the design had not yet addressed) found neither BPHS Ch. 26 nor Uttara Kalamrita Ch. 2 names Rahu or Ketu anywhere as an aspecting body -- a genuine silence in both texts. BPHS Ch. 27's explicit "the nodes are not considered" statement was found and considered, but it governs Shadbala scoring (a different chapter and purpose) and is explicitly not extended into a Ch. 26 Drishti-casting rule. Per the project owner's explicit instruction, this silence is not resolved by inference in either direction: both profiles return `NOT_EVALUABLE(aspecting_node_unresolved)` for a Rahu/Ketu aspecting body, calculating no value, while still accepting the request as structurally valid (a different handling from Chara Karaka's definitive, source-stated Ketu exclusion, which is a validation error, not a computed result). Affected calculations: none yet -- methodology and design only, no calculation code exists. Affected interpretations: none. Regression-test requirement (for the future implementation): Rahu and Ketu each return `NOT_EVALUABLE(aspecting_node_unresolved)` in both profiles; neither body is rejected as a malformed request; no Drishti value is produced; the behavior is deterministic; the two profiles are proven, by test, not to silently diverge on this case. Approval status: the research conclusion and this locked handling were both reviewed and explicitly approved by the project owner before this addendum; implementation has still not begun.
- **v1.14.0** (Phase 9 WP-D Western methodology lock, 2026-09-24): added §Western standards (WD-01 to WD-20): the tropical zodiac of date and half-open sign classification; three body profiles (classical seven, modern ten as the default, modern ten plus the lunar nodes as positions only with an explicit node convention); Chiron, asteroids and the Part of Fortune excluded; Placidus houses via Swiss Ephemeris with `NOT_EVALUABLE(placidus_polar_circle)` at |latitude| ≥ 90° − true obliquity and `NOT_EVALUABLE(placidus_not_computable)` for any other failure, never the Porphyry substitute; half-open house placement by ecliptic longitude; the five Ptolemaic aspects on the shorter arc with inclusive orbs and one aspect per pair; two orb profiles (the `WESTERN_ORB_FIXED_V1` engineering default and Lilly's per-planet moieties, which cannot evaluate the outer planets); applying/separating from instantaneous speeds; `NOT_EVALUABLE(birth_time_unknown)` for everything time-sensitive when the birth time is unknown. Recorded conflicts: Ptolemy's exclusion of the conjunction; Ptolemy's lack of orbs and the later Ashmand table; Lilly's planet-chapter orbs versus his own worked example. Also marked the Phase 1 Western/Tropical 'same aspect-detection mechanism' line as superseded. Reason: the project owner's WP-D directive (research, lock and implement in one execution). Affected calculations: new Western facts only; no Vedic calculation changes. Regression tests: all existing astro-engine and rule-engine suites. Approval status: approved by the project owner's WP-D directive.
- **v1.15.0** (Phase 9 WP-E KP methodology lock, 2026-09-25): added §KP standards (KP-01 to KP-16): the Krishnamurti ayanamsa default (Swiss Ephemeris mode 5, fitted to KP Reader I's table, applied only for the duration of a call) and the VP291 alternative; sidereal Placidus cusps with cusp-to-cusp houses and cusp-sign lordship; the Vimshottari-proportional star/sub division and the derived 249-entry table (202 Reader III rows agree), sub-sub as a derived calculation; four-level significators; Ruling Planets with a no-default day-lord convention; KP horary by number 1-249 with a numerically solved ARMC, checked against Reader VI's example 29; unknown time, polar latitudes and missing sunrise as `NOT_EVALUABLE`. Reason: the project owner's Phase 9 master directive (research, lock, implement, test, commit in one execution, as for WP-D). Affected: new KP results only. Regression: all earlier suites unchanged. Approval: owner directive.
- **v1.16.0** (Phase 9 WP-F Shadbala methodology lock, 2026-09-25): added §Shadbala standards (SB-01 to SB-20): one verse-literal profile over BPHS Ch. 27; eleven components evaluated (Uchcha, Saptavargaja, Ojayugma, Kendradi, Drekkana, Dig, Nathonnatha, Paksha, Tribhaga, Vara, Naisargika, and the Moon's Cheshta); Abda, Masa, Hora, Ayana, Yuddha, Drik and the other Cheshta values `NOT_EVALUABLE` with reasons, so no total is produced; Ch. 28 and Bhava Bala research-only. Reason: owner directive. Affected: new Shadbala results only; Phase 6 `requires_shadbala` rules are unchanged (no total exists). Approval: owner directive.
- **v1.17.0** (Phase 9 WP-G Jaimini methodology lock, 2026-09-25): added JN-11 to JN-18: planet-level Rashi Drishti (Ch. 8 v. 4-5) as its own profile, never blended with graha drishti; Bhava Pada with both exceptions and the recorded disagreements of the translator's worked chart; Graha Pada for the Sun and Moon only; Karakamsa from a caller-profiled Chara Karaka result. Reason: owner directive (Arudha and Karakamsa are not named in `Phases.md`; reported, P9-09). Approval: owner directive.
- **v1.18.0** (Phase 9 WP-H Chinese methodology lock, 2026-09-25): added §Chinese standards (CN-01 to CN-14): the Four Pillars calendar foundation; solar terms from the tropical Sun (all 216 HKO instants 2020-2028 within 30.2 s); year at 立春; month and hour stems from 《三命通會》 卷二; the sixty-day count; no-default time basis and day boundary; luck cycles not produced (they need the person's sex, not collected). Reason: owner directive. Approval: owner directive.
- **v1.19.0** (Phase 9 WP-I Tarot methodology lock, 2026-09-25): added §Tarot standards (TA-01 to TA-10): the Waite-Smith deck in Waite's numbering, Waite's Celtic method and two unlabelled engineering spreads, seeded SHA-256 draws with no system randomness, required reversal choice, explicit significator, preserved user selections, no meanings. Reason: owner directive. Approval: owner directive.
- **v1.20.0** (Phase 9 closure, 2026-09-25): added §Phase 9 closure (P9-01 to P9-09): Lal Kitab, Nadi, general Horary, Vastu, Feng Shui and Jaimini Dashas research-only or deferred with reasons; Ayurdaya excluded pending an owner product-policy decision; no Phase 9 package after WP-D integrated into the evidence bundle; the Arudha/Karakamsa roadmap gap reported. The Phase 1 KP, Lal Kitab, Nadi and "other systems" bullets gain pointers (text kept). Approval: owner directive.
- **v1.21.0** (Phase 9 closure and evidence hardening, 2026-09-25): added the modern Shadbala profile `SHADBALA_RAMAN_GRAHA_BHAVA_BALAS` (SR-01 to SR-24; B. V. Raman, *Graha and Bhava Balas*, page-image checked; every component checked on his printed worked example; two book contradictions exposed as no-default readings; book slips recorded) without changing the BPHS verse profile; the WP-G Jaimini facts object (JN-19, JN-20); Tarot provenance (TA-11); evidence-bundle integration of WP-E to WP-I (EV-01 to EV-10); the owner's Phase 9 decisions and status (P9-10, P9-11). Reason: owner directive of 2026-09-25. Affected: new Raman and Jaimini facts results, Tarot layouts (additive field), rule-engine bundles that carry the new sections. Regression: all earlier suites unchanged; bundles without the new sections hash as before. Approval: owner directive.

## Phase 1 standards checklist

Confirms all 13 `Phases.md` Phase 1 content requirements (exact wording preserved) are documented above as **standards**, not engines — each links to the phase that implements the corresponding engine:

- [x] Vedic astrology baseline — §Default Vedic profile, §Astrology systems (Vedic/Jyotish)
- [x] Lahiri ayanamsa — §Default Vedic profile
- [x] Sidereal zodiac — §Default Vedic profile
- [x] House calculation standards — §Default Vedic profile (Vedic whole-sign); §Astrology systems (Western Placidus, KP Placidus)
- [x] D1/D9/divisional-chart definitions — §Divisional charts (Vargas) — implemented Phase 5
- [x] Vimshottari Dasha methodology — §Vimshottari Dasha — implemented Phase 7
- [x] Nakshatra definitions — §Nakshatra standards — implemented Phase 5
- [x] Yoga/Dosha definitions — §Yoga/Dosha standards — implemented Phase 6
- [x] KP/Lal Kitab/Nadi/Western modules — §Astrology systems — implemented Phase 9
- [x] Panchang/Muhurta standards — §Panchang/Muhurta standards — implemented Phase 10
- [x] Numerology methodology — §Numerology standards — implemented Phase 11
- [x] Data/privacy rules — §Data & privacy principles (cross-reference to `PRODUCT_POLICIES.md`)
- [x] Prediction language policy — §Prediction language policy
