# @pandit-ji/ui

Reusable UI foundations for `apps/web` and `apps/admin` (`docs/ARCHITECTURE.md` §"Repository Structure").

## Phase 3 status

Package boundary only — no components yet. The first real component is added alongside the first web/admin screen that genuinely needs to share it, not speculatively now.
