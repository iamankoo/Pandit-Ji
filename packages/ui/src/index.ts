/**
 * @pandit-ji/ui -- reusable UI foundations shared by apps/web and apps/admin.
 *
 * Phase 3 scope: package boundary only. No components exist yet -- adding
 * one speculatively before a real screen needs it would be exactly the
 * "speculative abstraction" this foundation phase avoids. The first real
 * export lands alongside the first web/admin screen that needs to share it.
 */

export const PACKAGE_NAME = "@pandit-ji/ui";
