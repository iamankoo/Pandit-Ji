# Rule source-of-truth (`rules/*.yaml`)

This directory is the versioned YAML source-of-truth for astrology rules (`docs/ARCHITECTURE.md` §"Rule Engine Architecture"), mirrored into `rules.rule_definitions` at load time with a content hash as the version.

No rule content exists yet — the schema and first rules are authored starting `Phases.md` Phase 6, following `docs/ASTROLOGY_STANDARDS.md` §"Yoga / Dosha standards" exactly.
